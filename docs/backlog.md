# Backlog

This is the canonical product backlog for the current `0.3.x` line. It records
repo-evidenced status only; OpenFHE full-chain success is not claimed until a
single integrated encrypted chain runs without intermediate decrypts and emits
validated benchmark/error artifacts.

Status labels:

- Done: implemented and covered by committed tests, scripts, or recorded probe
  notes.
- Open: not yet implemented or not yet evidenced by an accepted artifact.
- Blocked: waiting on an explicit upstream PBI or hardware/library result.
- Obsolete: replaced by a narrower or more accurate PBI.

## Release Gate

The `0.3.0` gate was PBI-S1-005: an integrated tiny encrypted MIMO/SSD block
smoke. It was satisfied by `scripts/run_stage1_tiny_mimo_block_smoke.py` and the
recorded OpenFHE B200 job `10116` (`encrypted=true`, `passed=true`,
`max_abs_error=5.40e-13`). This does not claim real-checkpoint full-chain success.

## PBIs

| ID | Stage | Status | Depends On | Acceptance Criteria |
| --- | --- | --- | --- | --- |
| PBI-S0-001 | Stage 0 | Done | none | Tiny OpenFHE recurrence smoke encrypts per-token MIMO rank inputs, evaluates static scalar recurrence/readout, decrypts final outputs only, and is covered by `tests/test_openfhe_backend.py` plus README CLI docs. |
| PBI-S0-002 | Stage 0 | Done | PBI-S0-001 | Stage 0 MIMO benchmark harness supports OpenFHE and tracking backends with JSON output and tests in `tests/test_stage0_mimo.py` and `tests/test_stage0_sweep.py`. |
| PBI-S0-003 | Stage 0 | Done | PBI-S0-002 | Stage 0 status report aggregates measured artifacts, keeps `stage0_complete` false, and is covered by `tests/test_stage0_status.py` and `tests/test_stage0_status_script.py`. |
| PBI-S0-004 | Stage 0 | Done | PBI-S0-001 | OpenFHE bootstrap latency probe emits JSON and is accepted as a measured input, not a placeholder estimate; covered by `tests/test_bootstrap_latency.py`. |
| PBI-S0-005 | Stage 0 | Done | PBI-S0-001 | Real-checkpoint recurrence/bootstrap workflows are documented and scripted in `docs/checkpoint_workflows.md`, `scripts/run_openfhe_recurrence_chain_smoke.py`, and related script tests. |
| PBI-S0-006 | Stage 0 | Done | PBI-S0-005 | Source-profile, range scale plan, visible projection, and encrypted pre-recurrence/full-layer gate artifacts are represented in status reporting and script tests. |
| PBI-S0-007 | Stage 0 | Done | PBI-S0-006 | Reduced/synthetic or narrow full-layer ciphertext-chain proxies exist with explicit scope metadata and no full-model correctness claim; covered by chain script tests. Evidence now includes partial-visible proxy plumbing and rotation-guard artifacts in `v0.3.18`-`v0.3.19`, so oversized OpenFHE attempts emit JSON instead of empty failed jobs. |
| PBI-S0-008 | Stage 0 | Done | PBI-S0-006, PBI-S0-007 | Run a measured real-checkpoint full-layer ciphertext handoff chain at an agreed tiny size with no intermediate decrypts, validated max-error JSON, operation counts, and artifact validation. Attempts `10151`/`10153`/`10154` and `10164`-`10169` show depth exhaustion, HE-standard ring constraints, and 2-layer timeouts; `10170` proves one full visible OpenFHE layer can pass (`432.44s` wall clock, `max_abs_error=2.14e-02`) but does not provide inter-layer handoff. Partial-visible evidence: full inferred shape is blocked by rotation inventory (`10195`, `23498` keys / `11749.0 GiB`) and unstable in tracking (`10193`, `max_abs_error=3.45e111`); adapted shape passes tracking for 2 layers (`10196`, `max_abs_error=6.53e-04`), OpenFHE for 1 layer (`10198`, `max_abs_error=5.85e-04`), and OpenFHE for 2 layers with degree-3 SiLU/depth32/scale30 (`10204`, `max_abs_error=2.10e-02`, `ct_ct_mul=20`, `ct_pt_mul=3176`, wall `600.22s`). This closes the agreed tiny-size proxy, not full inferred shape or full visible output. |
| PBI-S0-009 | Stage 0 | Done | PBI-S0-008 | Scale the measured full-layer ciphertext handoff chain to the scheduled 24-layer recurrence plan or document a smaller proxy when cost is prohibitive; include encode/encrypt/eval/bootstrap/decrypt profiler breakdown. Evidence: `src/fhe_native_mamba3/stage0_closeout.py`, `scripts/build_stage0_closeout_report.py`, tests in `tests/test_stage0_closeout.py` / `tests/test_stage0_closeout_script.py`, and artifact `runs/stage0-s009-closeout-report-v0394.json`. Result: Stage 0 is closed at the current scoped objective as a blocker-identification/handoff stage, not as full 24-layer encrypted success. The closeout report records `close_current_stage0_scope=true`, `full_24_layer_success_claimed=false`, Mamba-130M setup rotations `163`, projected one-layer OpenFHE runtime about `9054.61s` by weighted ops, and hands execution to PBI-S1-041 plus later Stage 1/2 reductions. |
| PBI-S0-010 | Stage 0 | Done | PBI-S0-006 | Apply deterministic range-aware calibration where source-profile artifacts show residual/output ranges outside encoded targets. Evidence: repeat-prompt source profile `runs/st0-par-v0310-20260512-131549-source-profile-repeat.json`, scale plan `runs/st0-par-v0310-20260512-131549-source-profile-repeat-scale-plan.json`, and full-visible tracking gate `runs/stage0-s010-tracking-gate-l16-repeat-scaled-fullvisible-20260512-134254.json` (`passed=true`, `max_abs_error=1.05e-02`, `checked_visible_dim=768`). Activation-range LoRA remains a Stage 2 optimization under PBI-S2-009, not a Stage 0 blocker. |
| PBI-S1-001 | Stage 1 | Done | PBI-S0-003 | Stage 1 planning artifact combines SSD prefix-scan metadata, head/rank packing candidates, rotation inventory, key-memory estimates, and explicit dependencies; covered by `tests/test_stage1_plan.py` and `tests/test_stage1_plan_script.py`. |
| PBI-S1-002 | Stage 1 | Done | PBI-S1-001 | Packed SSD prefix-scan planning and local Hillis-Steele tracking kernel account for lane stride, slot capacity, rotation steps, and invalid cross-ciphertext scans; covered by `tests/test_ssd_prefix_scan.py`. |
| PBI-S1-003 | Stage 1 | Done | PBI-S1-002 | Segmented packed prefix-scan carry propagates across ciphertext chunks and updates rotation inventory; covered by `tests/test_ssd_prefix_scan.py`, `tests/test_rotation_inventory.py`, and `tests/test_stage1_plan.py`. |
| PBI-S1-004 | Stage 1 | Done | PBI-S1-003 | Prefix-scan smoke script runs segmented tracking end to end, persists JSON, and is covered by `tests/test_stage1_prefix_scan_smoke_script.py`. |
| PBI-S1-005 | Stage 1 | Done | PBI-S1-004 | Integrate tiny encrypted MIMO/SSD block smoke: combine encrypted tiny MIMO recurrence/readout with packed SSD/prefix-scan layout metadata, run without unsupported full-chain claims, emit benchmark/error JSON, and add a focused script test. Evidence: `tests/test_stage1_tiny_mimo.py`, `tests/test_stage1_tiny_mimo_block_smoke_script.py`, and OpenFHE B200 job `10116`. |
| PBI-S1-006 | Stage 1 | Done | PBI-S1-005 | Run head-pack/readout layout sweeps for 4, 8, 16, and 32 pack sizes with rotation-key count, memory estimate, latency/error JSON, and a clear recommendation. Evidence: `scripts/run_stage1_pack_sweep.py`, `tests/test_stage1_pack_sweep.py`, `tests/test_stage1_pack_sweep_script.py`, high/B200 tracking job `10117`, and OpenFHE non-power-of-two slot regression job `10118`. |
| PBI-S1-007 | Stage 1 | Done | PBI-S1-006, PBI-S0-004 | Attach measured FIDESlib/OpenFHE bootstrap availability and cost to Stage 1 layout choices; acceptance requires probe notes plus JSON artifacts that distinguish GPU bootstrap from OpenFHE Python bootstrap. Evidence: `native/fideslib_stage0/src/stage1_bootstrap_probe.cpp`, `scripts/run_fideslib_stage1_bootstrap_probe.py`, `slurm/fideslib_stage1_bootstrap_probe.sbatch`, tests in `tests/test_fideslib_stage1_bootstrap_probe_script.py`, and high/B200 job `10226` artifact `runs/stage1-s007-fideslib-target-bootstrap-v0349.json`. Result: Stage 1-sized FIDESlib GPU bootstrap probe (`ring_dimension=65536`, `num_slots=32768`, depth `28`, scale `40`, `security=128-classic`) passed as a cost/availability probe with `mean_latency_sec=0.0216`, precompute `16.47s`, plaintext load `199 ~ 2714MB`, rotation-key load `59 ~ 6548MB`, SLURM elapsed `28s`, MaxRSS `11757072K`; decrypt correctness was intentionally skipped and full-model correctness is not claimed. |
| PBI-S1-008 | Stage 1 | Done | PBI-S1-006 | Add an artifact-level Stage 1 comparison table that joins pack sweep rows, bootstrap latency attachments, rotation-key inventory, and high job IDs into one JSON/Markdown report. Evidence: `scripts/build_stage1_comparison_report.py`, `tests/test_stage1_report.py`, `tests/test_stage1_report_script.py`, and report artifact `runs/safe-v0315-20260512-063744-stage1-comparison-report.json`; this is report-only and does not claim real-checkpoint Stage 1 speedup. |
| PBI-S1-009 | Stage 1 | Done | PBI-S0-008 | Replace per-slot rank/state expansion rotations in encrypted pre-recurrence full-layer inventory with exact sparse BSGS expansion. Acceptance: full inferred Mamba-130M partial-visible chain inventory drops from `23498` rotations to `<800` without changing the mathematical result, and tiny expansion correctness is covered by tests. Evidence: `expand_rank_to_state_bsgs_rotation_steps`, `expand_state_vector_to_state_bsgs_rotation_steps`, tests in `tests/test_checkpoint_correctness.py` / `tests/test_checkpoint_encrypted_pre_recurrence_full_layer_script.py`, high guard artifact `10205` showing `rotation_count=713` / `139.26 GiB` at `200 MiB/key`, and OpenFHE small-rank execution artifact `10206` (`passed=true`, `max_abs_error=1.92e-02`). |
| PBI-S1-010 | Stage 1 | Done | PBI-S1-009 | Add an exact grouped-chain inventory for splitting full inferred Mamba-130M rank work into smaller rank packs before attempting heavy OpenFHE execution. Acceptance: report monolithic BSGS inventory plus pack-size rows for 4/8/16/32, with shared rotation-key count, estimated key memory, work multiplier, recommendation, tests, and a no-correctness-claim artifact. Evidence: `src/fhe_native_mamba3/stage1_grouped_chain.py`, `scripts/build_stage1_grouped_chain_inventory.py`, tests in `tests/test_stage1_grouped_chain.py` / `tests/test_stage1_grouped_chain_script.py`, and artifact `runs/stage1-s024-grouped-chain-inventory.json` showing monolithic `713` keys / `139.26 GiB` and pack-32 shared `136` keys / `26.56 GiB` with work multiplier `48`. |
| PBI-S1-011 | Stage 1 | Done | PBI-S1-010 | Add an executable grouped static MIMO recurrence smoke that slices rank work into packs, evaluates each pack, and reassembles final rank outputs against the monolithic plaintext recurrence. Acceptance: supports static and dynamic B/C/decay slices, emits artifact JSON with explicit no-full-layer claim, and is covered by unit/script tests. Evidence: `src/fhe_native_mamba3/stage1_grouped_recurrence.py`, `scripts/run_stage1_grouped_recurrence_smoke.py`, tests in `tests/test_stage1_grouped_recurrence.py` / `tests/test_stage1_grouped_recurrence_script.py`, tracking artifact `runs/stage1-s025-grouped-recurrence-smoke.json` (`pack_size=3`, `group_count=3`, `max_abs_error=2.78e-17`), and high/OpenFHE artifact `runs/stage1-s026-grouped-recurrence-openfhe-smoke.json` (`encrypted=true`, `max_abs_error=2.40e-10`). |
| PBI-S1-012 | Stage 1 | Done | PBI-S1-011 | Add grouped full-layer lift smoke: rank-pack recurrence outputs are gated, projected by matching out-projection columns, summed in visible slots, and residual is added. Acceptance: exact synthetic plaintext match, rotation inventory for recurrence plus visible projection, script artifact, and explicit no-checkpoint/full-model claim. Evidence: `run_stage1_grouped_full_layer_lift_smoke`, `scripts/run_stage1_grouped_full_layer_lift_smoke.py`, tests in `tests/test_stage1_grouped_recurrence.py` / `tests/test_stage1_grouped_full_layer_lift_script.py`, tracking artifact `runs/stage1-s028-grouped-full-layer-lift-tracking.json` (`pack_size=3`, `group_count=3`, `visible_dim=5`, `max_abs_error=2.78e-17`), and high/OpenFHE artifact `runs/stage1-s029-grouped-full-layer-lift-openfhe.json` (`encrypted=true`, `max_abs_error=8.57e-11`, `ct_ct_mul=4`). |
| PBI-S1-013 | Stage 1 | Done | PBI-S1-012 | Connect grouped full-layer lift to checkpoint pre-recurrence ciphertexts. Acceptance: full-rank encrypted pre-recurrence tensors can be compacted into rank packs, grouped recurrence/gate/out-projection/residual evaluates without intermediate decrypts, and a tiny checkpoint-shaped test matches visible output. Evidence: `run_checkpoint_grouped_encrypted_pre_recurrence_full_layer_gate` and `tests/test_checkpoint_correctness.py`; this still leaves full inferred-shape OpenFHE execution and pre-recurrence rank grouping as future work. |
| PBI-S1-014 | Stage 1 | Done | PBI-S1-013 | Script and artifact the checkpoint grouped full-layer gate. Acceptance: add a `scripts/run_checkpoint_grouped_encrypted_pre_recurrence_full_layer_gate.py` entrypoint, tests for JSON/schema/success semantics, one tracking artifact, and one small high/OpenFHE artifact with explicit no-full-model claim. Evidence: `scripts/run_checkpoint_grouped_encrypted_pre_recurrence_full_layer_gate.py`, `tests/test_checkpoint_grouped_encrypted_pre_recurrence_full_layer_script.py`, tracking artifact `runs/stage1-s014-grouped-checkpoint-gate-tracking.json` (`max_abs_error=9.87e-03`), and high/OpenFHE artifact `runs/stage1-s014-grouped-checkpoint-gate-openfhe.json` (`max_abs_error=1.46e-02`, `ct_ct_mul=20`, `rotations=60`). The OpenFHE artifact uses plaintext-exact RMSNorm/state decay to keep this a small proxy; full-rank encrypted pre-recurrence remains represented by the tracking artifact, and full-model correctness is not claimed. |
| PBI-S1-015 | Stage 1 | Done | PBI-S1-014, PBI-S1-010 | Add grouped checkpoint full-layer inventory and guard. Acceptance: report pack-size rows for checkpoint grouped gate at real Mamba-130M inferred shape, including pre-recurrence full-rank cost, grouped recurrence/lift cost, shared rotation keys, estimated key memory, and guard result before any heavy OpenFHE run. Evidence: `src/fhe_native_mamba3/stage1_checkpoint_grouped_gate.py`, `scripts/build_stage1_checkpoint_grouped_gate_inventory.py`, tests in `tests/test_stage1_checkpoint_grouped_gate.py` / `tests/test_stage1_checkpoint_grouped_gate_script.py`, and artifact `runs/stage1-s015-checkpoint-grouped-gate-inventory.json`. Result: full-rank pre-recurrence plus post-hoc grouped compaction is blocked under the 120 GiB guard; pack-32 still needs `1111` shared rotation keys / `216.99 GiB`, so PBI-S1-016 pre-recurrence rank grouping is now the main Stage 1 blocker. |
| PBI-S1-016 | Stage 1 | Done | PBI-S1-013 | Start pre-recurrence rank grouping. Acceptance: implement and test rank-pack slicing/compaction for checkpoint pre-recurrence ciphertext stages (`causal_conv_post_silu`, `gate_post_silu`, `dynamic_b/c`, `state_rank_decay`) without changing outputs; tracking tests must compare grouped vs full-rank pre-recurrence tensors. Evidence: `group_checkpoint_pre_recurrence_trace_by_rank`, `CheckpointPreRecurrenceGroupedCiphertextTrace`, and tests in `tests/test_checkpoint_pre_recurrence.py`; grouped checkpoint gate now consumes these rank-pack traces instead of doing ad hoc compaction inline. This still starts from full-rank projected tensors; the next slice is PBI-S1-017 chain proxy plus a follow-up inventory showing whether this grouping boundary is enough. |
| PBI-S1-017 | Stage 1 | Done | PBI-S1-014, PBI-S1-016 | Add grouped checkpoint full-layer chain proxy. Acceptance: two-layer partial-visible or small-shape chain uses grouped gate per layer, no intermediate decrypts, JSON artifact includes layer depth estimates, operation counts, and clear statement that full inferred 24-layer success is not claimed. Evidence: `run_checkpoint_grouped_encrypted_pre_recurrence_full_layer_ciphertexts_with_backend`, `run_checkpoint_grouped_encrypted_pre_recurrence_full_layer_chain_proxy`, `--grouped-rank-pack-size` support in `scripts/run_checkpoint_encrypted_pre_recurrence_full_layer_chain.py`, tests in `tests/test_checkpoint_correctness.py` / `tests/test_checkpoint_encrypted_pre_recurrence_full_layer_chain_script.py`, and artifact `runs/stage1-s017-grouped-checkpoint-chain-proxy-tracking.json` (`passed=true`, `layer_depth_estimates=[17,17]`, `ct_ct_mul=60`, `rotations=168`, final decrypt only). This is a tiny/checkpoint-shaped partial-visible proxy and explicitly does not claim full inferred 24-layer success. |
| PBI-S1-018 | Stage 1 | Done | PBI-S1-015, PBI-S1-017 | Run grouped full-shape guard/probe on high. Acceptance: full inferred Mamba-130M grouped gate/chain emits either a validated guard artifact under memory budget or a bounded OpenFHE artifact; no unguarded high-memory submission is allowed. Evidence: high job `10207`, artifact `runs/stage1-s018-20260513-022516-fullshape-grouped-chain-guard-openfhe-2l-v8.json`, and stdout `runs/stage1-s018-20260513-022516-fullshape-grouped-chain-guard-openfhe-2l-v8.stdout.json`. Result: guarded OpenFHE execution was blocked before backend setup with `rotation_count=1111`, estimated key memory `216.99 GiB` at `200 MiB/key`, and guard reason `estimated_rotation_key_memory`; artifact validates and explicitly does not claim success. |
| PBI-S1-019 | Stage 1 | Done | PBI-S1-007, PBI-S1-015 | Rebuild Stage 1 cost report with grouped checkpoint costs and measured bootstrap costs. Evidence: `src/fhe_native_mamba3/stage1_checkpoint_cost_report.py`, `scripts/build_stage1_checkpoint_cost_report.py`, tests in `tests/test_stage1_checkpoint_cost_report.py`, and report artifacts `runs/stage1-s019-checkpoint-cost-report.json` / `.md`. Result: report joins grouped inventory, PBI-S1-018 guard, PBI-S1-017 tracking proxy, measured OpenFHE Python bootstrap latency (`10.54s`), and Stage 1 target-shaped FIDESlib GPU bootstrap latency (`0.0216s`). `bootstrap_evidence_complete=true`; remaining blockers are `rotation_key_memory` and `estimated_rotation_key_memory`, not missing bootstrap evidence. |
| PBI-S1-020 | Stage 1 | Done | PBI-S1-018 | Add a composite-rotation diagnostic fallback for the current 1111-key grouped checkpoint inventory. Acceptance: implement signed-NAF decomposition, a backend wrapper that composes arbitrary rotations from basis keys, unit tests against the tracking backend, and a JSON report that marks the path as diagnostic rather than the final architecture. Evidence: `src/fhe_native_mamba3/composite_rotation.py`, `src/fhe_native_mamba3/stage1_composite_rotation_report.py`, `scripts/build_stage1_composite_rotation_report.py`, tests in `tests/test_composite_rotation.py` / `tests/test_stage1_composite_rotation_report.py` / `tests/test_stage1_composite_rotation_report_script.py`, and artifact `runs/stage1-s020-composite-rotation-diagnostic.json`. Result: the pack-32 grouped checkpoint inventory drops from `1111` logical rotation keys / `216.99 GiB` to `30` signed basis keys / `5.86 GiB`, but average logical rotation work increases to `3.40` key switches and this does not claim encrypted correctness or speed. |
| PBI-S1-021 | Stage 1 | Done | PBI-S1-019 | Add the preferred state-major rank-pack-first rotation planner. Acceptance: freeze the intended padded layout (`d_model_pad=1024`, `rank_pad=2048`, `d_state=16`), emit fixed BSGS schedules and state-axis shifts, fail closed above `150` application rotation keys, and record a planning artifact before HE execution. Evidence: `src/fhe_native_mamba3/stage1_state_major_layout.py`, `scripts/build_stage1_state_major_layout_plan.py`, tests in `tests/test_stage1_state_major_layout.py` / `tests/test_stage1_state_major_layout_script.py`, and artifact `runs/stage1-s021-state-major-layout-plan.json`. Result: the preferred layout plan emits `117` application rotation keys and `176` total keys including a `59`-key bootstrap allowance, with estimated total key memory `34.38 GiB` at `200 MiB/key`; this is planning-only and does not yet execute the layout. |
| PBI-S1-022 | Stage 1 | Done | PBI-S1-021 | Implement the first executable state-major layout kernel. Acceptance: a toy cleartext/tracking layout test must cover model-layout input, plaintext-exact projection references, state-major recurrence/readout shifts, model-layout output handoff, and an explicit guard that no rank-id-derived scatter rotations are used. Evidence: `src/fhe_native_mamba3/stage1_state_major_kernel.py`, `scripts/run_stage1_state_major_toy_kernel.py`, `slurm/stage1_state_major_toy_kernel.sbatch`, tests in `tests/test_stage1_state_major_kernel.py` / `tests/test_stage1_state_major_kernel_script.py`, local artifact `runs/stage1-s022-state-major-toy-kernel.json`, and high/OpenFHE job `10231` artifact `runs/stage1-s022-state-major-toy-openfhe-v0353.json`. Result: tracking recurrence/readout passes with `max_abs_error=0`; high/OpenFHE passes with `max_abs_error=3.58e-12`, state-reduce rotations `(8, 16)`, `ct_ct_mul=3`, `rotations=2`, and model-layout output handoff. Dense projections remain plaintext exact references in this slice; encrypted BSGS projection execution is the next PBI. |
| PBI-S1-023 | Stage 1 | Done | PBI-S1-022 | Replace the plaintext-exact projection references in the state-major toy kernel with a tracking BSGS projection primitive that uses only the fixed planner schedule. Acceptance requires exact toy output match and a runtime rotation trace proving no offset depends on concrete rank id or layer id. Evidence: `projection_mode="tracking-bsgs"` in `run_state_major_toy_kernel`, `--projection-mode tracking-bsgs` in `scripts/run_stage1_state_major_toy_kernel.py`, tests in `tests/test_stage1_state_major_kernel.py` / `tests/test_stage1_state_major_kernel_script.py`, and artifact `runs/stage1-s023-state-major-tracking-bsgs-projection.json`. Result: tracking-BSGS projection mode passes with `max_abs_error=0`, projection rotations `(-16, -8, 1, 2, 3, 4, 6)`, total runtime rotations `14`, and all projection rotations are a subset of the fixed planner schedule. Dense projection arithmetic is still exact-reference arithmetic while rotation traces are enforced; true slot-semantics/OpenFHE BSGS projection is PBI-S1-024. |
| PBI-S1-024 | Stage 1 | Done | PBI-S1-023 | Implement true slot-semantics BSGS projection for state-major layout. Acceptance requires a tracking primitive whose ciphertext values, not just its rotation trace, are produced by rotate/mul_plain/add operations, then a tiny OpenFHE projection smoke. Evidence: `projection_mode="slot-bsgs"` in `run_state_major_toy_kernel`, `required_state_major_toy_kernel_rotations`, tests in `tests/test_stage1_state_major_kernel.py` / `tests/test_stage1_state_major_kernel_script.py`, local artifact `runs/stage1-s024-state-major-slot-bsgs-projection-v0355.json`, and high/OpenFHE job `10232` artifact `runs/stage1-s024-state-major-slot-bsgs-openfhe-v0355.json`. Result: tracking slot-BSGS passes exactly with `max_abs_error=0`, runtime rotations `67`, `ct_pt_mul=99`; OpenFHE slot-BSGS passes with `max_abs_error=6.59e-11`, required application rotation keys `11`, runtime rotations `67`, SLURM elapsed `00:00:09`, and MaxRSS `400328K`. The true slot-semantics rotation set differs from the earlier planner trace, so full-shape planning must be refreshed before scaling. |
| PBI-S1-025 | Stage 1 | Done | PBI-S1-024 | Refresh the full-shape state-major rotation planner using the true slot-semantics BSGS schedule. Acceptance: derive the non-cyclic rectangular BSGS offsets for `Dpad=1024 -> Rpad=2048` and `Rpad=2048 -> Dpad=1024`, include state-block moves/reductions, compare against the earlier `117`-key planner, and fail closed if the application key count exceeds the memory guard. Evidence: `SlotBsgsSchedule`, `build_slot_bsgs_schedule`, default `build_state_major_layout_plan` slot-semantics mode, tests in `tests/test_stage1_state_major_layout.py` / `tests/test_stage1_state_major_layout_script.py`, and artifact `runs/stage1-s025-state-major-slot-bsgs-layout-plan-v0356.json`. Result: true slot-semantics planning emits `133` application rotation keys and `192` total keys including the `59`-key bootstrap allowance, estimated total key memory `37.50 GiB` at `200 MiB/key`, guard `allowed`; this supersedes the earlier `117`-key tracking-only planner for future full-shape runs. |
| PBI-S1-026 | Stage 1 | Done | PBI-S1-025 | Scale the state-major slot-BSGS kernel to full-shape tracking tensors. Acceptance: one layer with Mamba-130M-like dimensions (`d_model=768`, `rank_pad=2048`, `d_state=16`) uses slot-BSGS operations for x/gate/B/C/readout/out-projection in tracking mode, validates against a NumPy reference at each boundary, and records operation counts before any heavy OpenFHE run. Evidence: `src/fhe_native_mamba3/slot_bsgs.py`, `src/fhe_native_mamba3/stage1_state_major_fullshape.py`, `scripts/run_stage1_state_major_fullshape_tracking.py`, `slurm/stage1_state_major_fullshape_tracking.sbatch`, tests in `tests/test_stage1_state_major_fullshape.py`, and high job `10250` artifact `runs/stage1-s026-fullshape-state-major-tracking-v0360.json`. Result: full-shape synthetic Mamba-130M-like tracking run passes with `max_abs_error=3.12e-17`, boundary errors reported for `x/gate/b/c/state_new/readout_rank/output_model`, required application rotation keys `133`, runtime rotations `3502`, `ct_pt_mul=80605`, `ct_ct_mul=4`, elapsed `00:00:09`, and MaxRSS `388000K`. |
| PBI-S1-027 | Stage 1 | Done | PBI-S1-026, PBI-S0-010 | Connect the state-major rank-pack-first kernel to a real checkpoint layer. Acceptance: adapt one Mamba-family checkpoint layer into the state-major kernel shape, keep inter-layer handoff in model layout, compare boundary tensors against the existing checkpoint reference, and explicitly separate checkpoint-adapter error from kernel/FHE error. Evidence: `src/fhe_native_mamba3/stage1_state_major_checkpoint.py`, `scripts/run_stage1_state_major_checkpoint_layer_tracking.py`, tests in `tests/test_stage1_state_major_checkpoint.py`, and artifact `runs/stage1-s027-state-major-checkpoint-layer-tracking-v0361.json`. Result: one tiny checkpoint-shaped layer passes through source pre-recurrence boundary tensors, encrypted/tracking recurrence/readout/out-projection, and model-layout output handoff with `checkpoint_adapter_max_abs_error=1.20e-07` and `kernel_max_abs_error=1.11e-16`; full encrypted pre-recurrence and full Mamba-130M checkpoint execution remain future work. |
| PBI-S1-028 | Stage 1 | Done | PBI-S1-027 | Move the first checkpoint pre-recurrence tensors into the state-major rank-pack-first graph. Acceptance: compute `rank_input` and `gate` from RMSNorm output using state-major BSGS plus SiLU polynomial, report per-boundary errors versus `_run_source_dynamic_formula`, and keep model-layout handoff intact while B/C/decay remain source-boundary inputs. Evidence: `pre_recurrence_mode="rank-gate-bsgs-poly"` in `run_state_major_checkpoint_layer_tracking`, tests in `tests/test_stage1_state_major_checkpoint.py`, and artifact `runs/stage1-s028-rank-gate-bsgs-poly-tracking-v0362.json`. Result: tiny checkpoint-shaped layer passes with degree-15/range-8 SiLU, `kernel_max_abs_error=1.79e-02`, `rank_input` error `5.08e-03`, `gate` error `3.32e-03`, `ct_ct_mul=32`, and model-layout output handoff preserved. |
| PBI-S1-029 | Stage 1 | Done | PBI-S1-028 | Move dynamic `B/C` into the state-major graph. Acceptance: derive B/C from computed `rank_input` without source-boundary injection, broadcast the state-vector outputs into state-major layout, report per-boundary errors against `_run_source_dynamic_formula`, and keep decay as the only source-boundary input. Evidence: `pre_recurrence_mode="rank-gate-bc-bsgs-poly"` in `run_state_major_checkpoint_layer_tracking`, tests in `tests/test_stage1_state_major_checkpoint.py`, and artifact `runs/stage1-s029-rank-gate-bc-bsgs-poly-tracking-v0363.json`. Result: tiny checkpoint-shaped layer passes with `kernel_max_abs_error=6.50e-02`, B/C errors `5.53e-03` / `7.66e-03`, runtime rotations `44`, `ct_ct_mul=32`, and required application rotations `10`; state-rank decay remains source-boundary input. |
| PBI-S1-030 | Stage 1 | Done | PBI-S1-029 | Move state-rank decay into the state-major graph. Acceptance: compute dt projection and exp/softplus decay approximation from computed `rank_input`, produce state-major decay slots without source-boundary injection, and report decay/readout/output errors. Evidence: `pre_recurrence_mode="rank-gate-bc-decay-bsgs-poly"` in `run_state_major_checkpoint_layer_tracking`, tests in `tests/test_stage1_state_major_checkpoint.py`, and artifact `runs/stage1-s030-rank-gate-bc-decay-bsgs-poly-tracking-v0364.json`. Result: tiny checkpoint-shaped one-token bridge has no source-boundary pre-recurrence tensors, `decay` error `4.04e-05`, `kernel_max_abs_error=6.50e-02`, `ct_ct_mul=37`, and runtime rotations `55`; because the previous state is zero, decay is boundary-checked but not yet stress-tested through a nonzero recurrent state. |
| PBI-S1-031 | Stage 1 | Done | PBI-S1-030 | Validate state-major checkpoint pre-recurrence with nonzero recurrent state or a second token. Acceptance: run at least a nonzero-state tiny checkpoint trace where computed decay affects `state_new`, compare state/readout/output against the state-major checkpoint reference, and emit explicit scope metadata before attempting OpenFHE. Evidence: `--previous-state-scale` support in `scripts/run_stage1_state_major_checkpoint_layer_tracking.py`, tests in `tests/test_stage1_state_major_checkpoint.py`, and artifact `runs/stage1-s031-nonzero-state-decay-check-v0365.json`. Result: nonzero previous state check passes with `decay_effect_checked=true`, no source-boundary pre-recurrence tensors, `state_new` error `2.74e-03`, `decay` error `4.04e-05`, `kernel_max_abs_error=6.44e-02`, `ct_ct_mul=37`, and runtime rotations `55`. |
| PBI-S1-032 | Stage 1 | Done | PBI-S1-031 | Add a tiny OpenFHE execution path for the full state-major checkpoint bridge. Acceptance: expose backend selection for `run_stage1_state_major_checkpoint_layer_tracking.py` or a sibling script, run a tiny shape on high with required rotations from the planner, and emit diagnostic JSON with the same boundary/scope metadata. Evidence: high/B200 job `10259`, artifact `runs/stage1-s032-retry-deg11-s40-d48-v0368.json`, and script/test support in `v0.3.66`/`v0.3.68`. Result: tiny OpenFHE state-major checkpoint bridge passes with degree-11 SiLU, scale `40`, depth `48`, ring `131072`, `max_abs_error=3.91e-01`, `ct_ct_mul=29`, runtime rotations `55`, required application rotations `10`, setup `19.19s`, MaxRSS `6890960K`. Degree-13/15 are still accuracy targets but currently hit CKKS precision/decrypt failure, so higher-accuracy polynomial recovery is split to PBI-S1-035. Boundary decrypts are allowed for this one-layer diagnostic; final-decrypt-only remains a later chain-level claim. |
| PBI-S1-033 | Stage 1 | Done | PBI-S1-031 | Add a tiny tracking chain for state-major checkpoint bridges. Acceptance: chain at least two checkpoint-shaped layers, keep the model-layout output ciphertext as the next layer residual input, explicitly mark plaintext-reference pre-recurrence inputs, and emit JSON with layer-wise error growth and operation counts. Evidence: `run_state_major_checkpoint_chain_tracking`, `scripts/run_stage1_state_major_checkpoint_chain_tracking.py`, tests in `tests/test_stage1_state_major_checkpoint.py`, and artifact `runs/stage1-s033-state-major-checkpoint-chain-tracking-v0367.json`. Result: 2-layer tiny chain passes with layer errors `(6.44e-02, 1.19e-01)`, required application rotations `10`, `ct_ct_mul=74`, runtime rotations `110`, and the scope explicitly does not claim full encrypted pre-recurrence or full-model correctness. |
| PBI-S1-034 | Stage 1 | Done | PBI-S1-032 | Add a tracking polynomial-degree sweep for the state-major checkpoint bridge. Acceptance: sweep candidate SiLU polynomial degrees, report error/work tradeoffs, choose the lowest degree under a configured max error, and keep the artifact tracking-only. Evidence: `scripts/run_stage1_state_major_checkpoint_poly_sweep.py` and tests in `tests/test_stage1_state_major_checkpoint.py`. Result: the tiny checkpoint bridge recommends degree `11` under max error `0.5`; degree `15` is still the accuracy target, but OpenFHE retry work needs the lower-degree diagnostic path when CKKS precision fails. |
| PBI-S1-035 | Stage 1 | Done | PBI-S1-032, PBI-S1-034 | Recover higher-accuracy OpenFHE polynomial settings for the state-major checkpoint bridge. Acceptance: either make degree-13/15 decrypt successfully with a justified CKKS parameter/scheduling change, or document a lower-degree/range-calibrated path with measured error improvement over degree-11; artifacts must include failure JSON for rejected configurations and a successful OpenFHE JSON for the chosen path. Evidence: local tracking artifact `runs/stage1-s035-mixed-gate-degree-tracking-v0370.json`, high failure artifact `runs/stage1-s035-mixed-rank15-gate11-openfhe-v0371.json` from job `10286`, and high success artifact `runs/stage1-s035-mixed-rank15-gate9-openfhe-v0371.json` from job `10287`. Result: rank polynomial degree `15` with gate degree `9` passes OpenFHE at scale `40`, depth `48`, ring `131072`, `max_abs_error=6.59e-02`, `ct_ct_mul=31`, runtime rotations `55`, required application rotations `10`, SLURM elapsed `00:03:09`, MaxRSS `6724628K`. The rejected rank-15/gate-11 OpenFHE row decrypted but exceeded `atol=8e-2` with `max_abs_error=1.07e-01`, so the accepted path is the lower-depth mixed-degree gate calibration. |
| PBI-S1-036 | Stage 1 | Done | PBI-S1-033, PBI-S1-035 | Run a tiny OpenFHE chain over the mixed-degree state-major checkpoint bridge. Acceptance: execute at least a 2-layer chain using rank degree `15` and gate degree `9`, keep inter-layer handoff in model layout, emit layer-wise errors and operation counts, and explicitly mark whether pre-recurrence is fully encrypted or still uses reference inputs at each boundary. Evidence: script/backend support in `v0.3.73` and high/B200 job `10288`, artifact `runs/stage1-s036-mixed-chain-openfhe-v0373.json`. Result: 2-layer tiny OpenFHE chain passes with rank degree `15`, gate degree `9`, scale `40`, depth `48`, ring `131072`, layer errors `(6.61e-02, 1.17e-01)`, `max_abs_error=1.17e-01`, `ct_ct_mul=62`, runtime rotations `110`, required application rotations `10`, SLURM elapsed `00:05:55`, MaxRSS `6724912K`. The artifact explicitly marks `pre_recurrence_plaintext_reference_input=true`; full encrypted pre-recurrence chain correctness is not claimed. |
| PBI-S1-037 | Stage 1 | Done | PBI-S1-036, PBI-S1-025 | Add a guarded OpenFHE scale sweep for the mixed-degree state-major checkpoint bridge. Acceptance: run or dry-run a shape ladder beyond the tiny `d_model=8/rank=6/d_state=2` case, record planned rotation keys, estimated key memory, actual peak memory for completed jobs, and stop before any configuration exceeds the memory guard. Evidence: `src/fhe_native_mamba3/stage1_openfhe_scale_sweep.py`, `scripts/build_stage1_openfhe_scale_sweep.py`, tests in `tests/test_stage1_openfhe_scale_sweep.py`, and artifact `runs/stage1-s037-openfhe-scale-sweep-v0378.json`. Result: dry-run ladder covers tiny/small/medium/mamba130m including `dt_proj` rotations; checkpoint application rotation keys are `10/42/68/163`, estimated total key memory is `13.48/19.73/24.80/43.36 GiB`, all rows pass the guard, and the tiny completed row imports job `10288` with MaxRSS `6724912K`. |
| PBI-S1-038 | Stage 1 | Done | PBI-S1-037 | Execute the first guarded small-shape OpenFHE checkpoint bridge. Acceptance: prepare or generate a checkpoint-shaped artifact for the `small` scale-sweep row (`d_model=64`, `mimo_rank=96`, `rank_pad=128`, `d_state=4`), run at least a 1-layer mixed-degree OpenFHE bridge, record error/runtime/MaxRSS, and compare observed memory against the PBI-S1-037 estimate. Evidence: synthetic checkpoint builder in `v0.3.76`, dt-rotation inventory fixes in `v0.3.78`/`v0.3.79`, high diagnostic failure `10295`, and high/B200 success job `10297`, artifact `runs/stage1-s038-small-layer-openfhe-v0379.json`. Result: small 1-layer OpenFHE bridge passes with `d_model=64`, `mimo_rank=96`, `rank_pad=128`, `d_state=4`, `dt_rank=8`, rank degree `15`, gate degree `9`, scale `40`, depth `48`, ring `131072`, `max_abs_error=5.06e-02`, required application rotations `42`, runtime rotations `222`, `ct_pt_mul=890`, `ct_ct_mul=31`, SLURM elapsed `00:13:34`, MaxRSS `19705908K`. Observed MaxRSS is about `18.8 GiB`, close to the PBI-S1-037 small estimate `19.73 GiB`. |
| PBI-S1-039 | Stage 1 | Done | PBI-S1-038 | Execute the guarded medium-shape OpenFHE checkpoint bridge or produce a bounded failure artifact. Acceptance: prepare the `medium` scale-sweep checkpoint (`d_model=128`, `mimo_rank=192`, `rank_pad=256`, `d_state=8`, `dt_rank=16`), run a 1-layer mixed-degree OpenFHE bridge under the memory guard, and record pass/fail, elapsed time, MaxRSS, and whether the observed memory tracks the PBI-S1-037 estimate. Evidence: high/B200 job `10298`, artifact `runs/stage1-s039-medium-layer-openfhe-v0380.json`. Result: medium 1-layer OpenFHE bridge passes with `d_model=128`, `mimo_rank=192`, `rank_pad=256`, `d_state=8`, `dt_rank=16`, rank degree `15`, gate degree `9`, scale `40`, depth `48`, ring `131072`, `max_abs_error=5.06e-02`, required application rotations `68`, runtime rotations `397`, `ct_pt_mul=1786`, `ct_ct_mul=31`, SLURM elapsed `00:25:07`, MaxRSS `30444564K`. Observed MaxRSS is about `29.0 GiB`, moderately above the PBI-S1-037 medium estimate `24.80 GiB` but still safely under the `128G` request. |
| PBI-S1-040 | Stage 1 | Done | PBI-S1-039 | Attempt the guarded Mamba-130M-shape OpenFHE checkpoint bridge or produce a bounded failure artifact. Acceptance: prepare the `mamba130m` scale-sweep checkpoint (`d_model=768`, `mimo_rank=1536`, `rank_pad=2048`, `d_state=16`, `dt_rank=48`), run at least a setup/keygen or one-layer mixed-degree OpenFHE bridge under an explicit memory/time guard, and record pass/fail, elapsed time, MaxRSS, required rotations, and whether observed memory follows the PBI-S1-037 estimate. Evidence: high/B200 job `10299`, artifact `runs/stage1-s040-mamba130m-setup-openfhe-v0382.json`, plus local tracking artifact `runs/stage1-s040-mamba130m-layer-tracking-v0381.json`. Result: setup/keygen-only OpenFHE probe passes with `d_model=768`, `mimo_rank=1536`, `rank_pad=2048`, `d_state=16`, `dt_rank=48`, scale `40`, depth `48`, ring `131072`, required application rotations `163`, backend batch size `32768`, setup `242.53s`, SLURM elapsed `00:04:08`, MaxRSS `63342248K` (about `60.4 GiB`). This is above the PBI-S1-037 estimate `43.36 GiB` but under the explicit `128G` guard; no layer evaluation or full-model correctness is claimed. |
| PBI-S1-041 | Stage 1 | Done | PBI-S1-040 | Attempt a bounded Mamba-130M-shape one-layer OpenFHE evaluation or record an explicit runtime/memory stop condition. Evidence: high/B200 job `10300`, artifact `runs/stage1-s041-mamba130m-layer-openfhe-v0384.json`, collection artifact `runs/stage1-s041-collection-v0396.json`, and local runtime projection `runs/stage1-s041-runtime-projection-v0385.json`. Result: the Mamba-130M-shape synthetic checkpoint one-layer OpenFHE path passes with `d_model=768`, `mimo_rank=1536`, `rank_pad=2048`, `d_state=16`, `dt_rank=48`, rank degree `15`, gate degree `9`, scale `40`, depth `48`, ring `131072`, `max_abs_error=5.06e-02`, required application rotations `163`, runtime rotations `1028`, `ct_pt_mul=13210`, `ct_ct_mul=31`, total runtime `8694.12s`, SLURM elapsed `02:25:01`, and MaxRSS `70890876K` (about `67.6 GiB`). This validates one encrypted checkpoint-shaped layer and model-layout output handoff; full 24-layer or multi-layer Mamba-130M success remains unclaimed. |
| PBI-S1-042 | Stage 1 | Done | PBI-S1-041 | Decide the next scaling step after the Mamba-130M one-layer OpenFHE result. Evidence: `src/fhe_native_mamba3/stage1_scaling_decision.py`, `scripts/build_stage1_scaling_decision.py`, tests in `tests/test_stage1_scaling_decision.py` / `tests/test_stage1_scaling_decision_script.py`, and artifact `runs/stage1-s042-scaling-decision-v0396.json`. Result: measured one-layer runtime is `8694.12s`, MaxRSS is about `67.6 GiB`, the measured/projection ratio is `0.96`, projected 2-layer runtime is `17388.24s`, projected 24-layer runtime is `208658.93s`, and the recommendation is `prioritize_fideslib_or_sketch_before_direct_multilayer_openfhe`. Memory is acceptable; runtime/projection work and missing multi-layer bootstrap scheduling are the blockers. |
| PBI-S1-043 | Stage 1 | Done | PBI-S1-042, PBI-S1-007 | Start the FIDESlib/state-major scaling path before direct multi-layer OpenFHE. Acceptance: implement a bounded FIDESlib/native or FIDESlib-compatible probe for the state-major Mamba-130M one-layer schedule, starting with the 163-key rotation inventory and a representative BSGS projection/rotation group; record key memory, runtime, and whether the OpenFHE 8694s one-layer bottleneck is plausibly reducible. Evidence: `native/fideslib_stage0/src/stage1_rotation_probe.cpp`, `scripts/run_fideslib_stage1_rotation_probe.py`, `slurm/fideslib_stage1_rotation_probe.sbatch`, helper module `src/fhe_native_mamba3/stage1_fideslib_rotation_probe.py`, tests in `tests/test_stage1_fideslib_rotation_probe.py` / `tests/test_fideslib_stage1_rotation_probe_script.py`, high/B200 target job `10331`, artifact `runs/stage1-s043-fideslib-rotation-target-v03101.json`, and collection `runs/stage1-s043-target-collection-v03101.json`. Result: the target `ring=131072`, `slots=32768`, depth `48`, scale `40`, 163-key FIDESlib rotation/key-memory probe passed with `stage1_state_major_target_compatible=true`, rotate-keygen `58.28s`, context load `84.65s`, representative 163-rotation plus plaintext-multiply group `0.069s`, peak RSS `68.35 GiB`, and SLURM MaxRSS `71674360K`. Diagnostics also passed for 1-key ring65536/depth28 (`10328`, peak `1.99 GiB`) and 163-key ring65536/depth28 (`10330`, peak `20.06 GiB`). This proves the FIDESlib primitive path is viable; full one-layer Mamba checkpoint execution is not yet claimed. |
| PBI-S1-044 | Stage 1 | Done | PBI-S1-043, PBI-S1-041 | Measure the FIDESlib native projection/eval op mix for the Mamba-130M one-layer state-major bridge before porting full correctness. Acceptance: implement a bounded native/FIDESlib projection-cost probe that exercises the same class and approximate count of operations as PBI-S1-041 (`rotations ~= 1028`, `ct_pt_mul ~= 13210`, `ct_ct_mul=31`, no bootstrap) using synthetic plaintext diagonals and state-major layouts; emit runtime/memory/error-scope JSON and compare the observed cost to the OpenFHE `8694.12s` one-layer artifact. Evidence: op-mix mode in `native/fideslib_stage0/src/stage1_rotation_probe.cpp`, SLURM job `10334`, artifact `runs/stage1-s044-fideslib-opmix-v03103.json`, and collection `runs/stage1-s044-opmix-collection-v03103.json`. Result: target-compatible FIDESlib op-mix proxy passed with `rotations=1028`, `ct_pt_mul=13210`, `ct_ct_mul=31`, no bootstrap, eval time `3.61s`, rotate keygen `62.70s`, context load `87.23s`, peak RSS `68.35 GiB`, SLURM MaxRSS `71674376K`, and elapsed `00:02:49`. Compared with OpenFHE PBI-S1-041 one-layer runtime `8694.12s`, this strongly supports continuing to a FIDESlib correctness port rather than routing immediately to Stage 2 sketch reduction. |
| PBI-S1-045 | Stage 1 | In Progress | PBI-S1-044, PBI-S1-041 | Port the Mamba-130M-shape state-major one-layer checkpoint bridge to a correctness-carrying FIDESlib/native path. Acceptance: consume the same synthetic checkpoint-shaped tensors and 163-key state-major inventory as PBI-S1-041, execute the one-layer recurrence/readout/gate/out-projection path natively or through a thin FIDESlib wrapper, and emit JSON with final or boundary decrypt comparison against the existing tracking/OpenFHE reference. The artifact must explicitly separate full one-layer correctness from full 24-layer or full-model claims. Current slice: Python exports deterministic checkpoint tail payloads (`scripts/export_stage1_checkpoint_tail_payload.py`), native C++ parses/evaluates the tail, tiny FIDESlib encrypted tail job `10340` passed, and Mamba-130M-shaped encrypted tail job `10341` passed with `max_abs_error=0`, `eval_seconds=242.85`, `ct_pt_mul=2307`, `ct_ct_mul=1`, and MaxRSS about `48.0 GiB`; the gap report leaves `922` rotations, `10903` plaintext multiplications, and `30` ciphertext multiplications in pre-recurrence/full-layer work. Version `0.3.109` adds a rank/gate pre-recurrence payload/native eval slice (`scripts/export_stage1_rank_gate_payload.py`) with Mamba-130M-shaped native parity `max_abs_error=5.46e-07`. Version `0.3.110` adds encrypted FIDESlib rank/gate projection: small shape job `10360` passes in `4.34s` eval, and Mamba-130M-shaped job `10358` passes with `102` keys, `ct_pt_mul=4606`, eval `326.20s`, peak `46.56 GiB`, and `max_abs_error=2.44e-07`; the Mamba row uses a documented rank-projection scale for tiny synthetic coefficients. Version `0.3.111` adds encrypted polynomial SiLU and skip update to the rank/gate FIDESlib slice; small shape job `10364` passes with `ct_ct_mul=22`, eval `26.46s`, peak `18.65 GiB`, and encrypted-vs-polynomial-reference `max_abs_error=6.55e-08`; Mamba-130M-shaped job `10365` passes with `102` keys, `ct_pt_mul=4608`, `ct_ct_mul=22`, eval `327.17s`, peak `46.55 GiB`, and `max_abs_error=1.19e-06` against the polynomial reference. Version `0.3.113` extends the payload/native kernel to dynamic `B/C` from polynomial rank input and state-major B/C broadcast; tiny job `10376` passes with `9` keys, `ct_pt_mul=45`, `ct_ct_mul=22`, eval `13.72s`, peak `13.29 GiB`, and `max_abs_error=0`; small96 job `10380` passes with `47` keys, `ct_pt_mul=525`, eval `42.19s`, peak `26.87 GiB`, and `max_abs_error=1.41e-07`; Mamba-130M-shaped job `10378` passes with `165` keys, `ct_pt_mul=7742`, `ct_ct_mul=22`, eval `536.61s`, peak `69.08 GiB`, and `max_abs_error=3.59e-06`. Version `0.3.114` adds encrypted `dt` projection and state-major decay polynomial to the same slice, with `dt_projection_scale=100` and tiny-coefficient floor handling; tiny job `10402` passes with `9` keys, `ct_pt_mul=61`, `ct_ct_mul=27`, eval `18.94s`, peak `13.29 GiB`, `max_abs_error=0`, and `decay_poly_vs_exact_max_abs_error=4.04e-05`; small96 job `10404` passes with `54` keys, `ct_pt_mul=731`, `ct_ct_mul=27`, eval `59.48s`, peak `29.38 GiB`, and `max_abs_error=1.27e-07`. Mamba-130M decay job `10405` passes with `185` keys, `ct_pt_mul=10908`, `ct_ct_mul=27`, setup `165.28s`, eval `776.73s`, peak `76.23 GiB`, `max_abs_error=1.66e-06`, and `decay_poly_vs_exact_max_abs_error=3.74e-05`. Version `0.3.115` starts recurrence/readout/out-projection tail integration inside the same FIDESlib kernel; zero-state tiny job `10419` passes final polynomial output with `max_abs_error=0`, `ct_ct_mul=30`, `rotations=55`, eval `21.10s`, and peak `13.64 GiB`; small96 job `10421` passes with `max_abs_error=0`, `ct_ct_mul=30`, `rotations=222`, eval `70.04s`, and peak `25.09 GiB`. Nonzero-state tiny job `10416` reaches the recurrence/readout/output tail but fails CKKS decrypt with approximation error too high after `ct_ct_mul=31`; low-depth nonzero retry `10424` passes with rank/gate/decay degrees `13/7/3`, `ct_ct_mul=25`, eval `19.45s`, and `max_abs_error=0`; small96 low-depth nonzero job `10425` also passes with `ct_ct_mul=25`, eval `70.37s`, peak `25.13 GiB`, and `max_abs_error=0`, so encrypted previous-state handoff is formula-correct but needs depth/bootstrap scheduling at the higher-accuracy polynomial setting. Mamba-130M zero-state full-tail job `10423` passes with `189` keys, `rotations=1036`, `ct_pt_mul=13212`, `ct_ct_mul=30`, setup `170.64s`, eval `1033.71s`, peak `77.67 GiB`, and final `max_abs_error=0`; remaining Stage 1 blockers are high-degree nonzero-state handoff, bootstrap/level scheduling, and multi-layer chaining. |
| PBI-S2-001 | Stage 2 | Done | PBI-S1-005 | Empirically test sketch dimensions for MIMO SSM trajectories before claiming theory-driven dimension choices. Evidence: synthetic SRHT sweep, checkpoint-source trace ingestion, and multi-seed sketch aggregation are implemented in `scripts/run_stage2_sketch_sweep.py`, `scripts/run_stage2_sketch_seed_sweep.py`, `scripts/run_checkpoint_source_sketch_trace.py`, `tests/test_stage2_sketch_sweep.py`, `tests/test_stage2_sketch_seed_sweep.py`, and `tests/test_checkpoint_sketch_trace.py`; recorded high/B200 artifacts `runs/checkpoint-source-sketch-trace-mamba130m-l0-20260512-081037.json` and `runs/stage2-sketch-seed-sweep-mamba130m-l0-trace-20260512-124154.json` show layer-0 Mamba-130M selected-rank tradeoffs. |
| PBI-S2-002 | Stage 2 | Open | PBI-S0-010, PBI-S1-005 | Prototype lazy bootstrap/range-aware training policy with measured range contraction and bootstrap schedule impact. This is an umbrella PBI; the executable slices are PBI-S2-008 and PBI-S2-009. |
| PBI-S2-003 | Stage 2 | Done | PBI-S1-005 | Track encrypted vocab argmax/CutMax separately from the default client-side decoding path. Evidence is split into PBI-S2-010 for client-side decode accounting and PBI-S2-011 for a toy encrypted CutMax path. This closes the branch as scoped; full-vocab encrypted generation remains explicitly unclaimed. |
| PBI-S2-004 | Stage 2 | Done | PBI-S2-012 | Expand checkpoint-derived sketch evidence beyond Mamba-130M layer 0. Evidence: high/B200 job `10135`, `runs/checkpoint-sketch-matrix-mamba130m-20260512-130750.json`, layers `0,12,23`, prompts `short/repeat`, rank strategies `first:8` and `stride:8:64`, sketch sizes `4/8/16`, seeds `0..4`. Result: artifact `passed=true`; recommended sketch is full-width `16` for 11/12 rows and `8` for 1/12 row; size 4 min pass-rate `0.0`, size 8 min pass-rate `0.4`, size 16 min pass-rate `1.0`. |
| PBI-S2-005 | Stage 2 | Done | PBI-S2-001 | Implement learned or data-dependent sketch baselines (PCA/SVD or trainable projection) against the same checkpoint trace format. Acceptance requires before/after comparison versus SRHT on the same trace artifacts and explicit metadata marking plaintext/offline training only. Evidence: `src/fhe_native_mamba3/stage2_learned_sketch_baseline.py`, `scripts/run_stage2_learned_sketch_baseline.py`, tests in `tests/test_stage2_learned_sketch_baseline.py`, and artifact `runs/stage2-s005a-learned-sketch-baseline-v0386.json`. Result: offline PCA/SVD baseline on checkpoint trace `runs/checkpoint-source-sketch-trace-mamba130m-l0-20260512-081037.json` passes at sketch sizes `4/8/16`; size `4` has pairnorm L2 error `2.11e-02`, size `8` has `4.98e-03`, while matched SRHT needs size `16` to pass all seeds. Payload explicitly marks plaintext/offline training, data-dependent projection, no encrypted execution, and no full-model correctness claim. |
| PBI-S2-006 | Stage 2 | Done | PBI-S2-001, PBI-S1-005 | Add a backend smoke for SRHT sketch primitives over encrypted packed state vectors. Acceptance requires Tracking and OpenFHE tiny-state runs that validate sign flip, Hadamard rotations, sampling mask, rotation inventory, and zero multiplicative-depth accounting. Evidence: `src/fhe_native_mamba3/backend_srht.py`, `scripts/run_stage2_backend_srht_smoke.py`, tests in `tests/test_backend_srht.py`, tracking artifact `runs/stage2-s006-backend-srht-tracking.json`, and high/OpenFHE job `10209` artifact `runs/stage2-s006-backend-srht-openfhe.json`. Result: Tracking state_width 8 passes with `max_abs_error=0`, rotations `[-4,-2,-1,1,2,4]`, `ct_ct_mul=0`, `bootstraps=0`; OpenFHE state_width 4 passes with `max_abs_error=1.76e-10`, rotations `[-2,-1,1,2]`, `ct_ct_mul=0`, `bootstraps=0`, elapsed `5s`, MaxRSS `501640K`. |
| PBI-S2-007 | Stage 2 | Done | PBI-S2-001 | Split sketch claims by recurrence type: scalar decay, rank-scalar decay, and rank-state decay. Acceptance requires code-level flags and tests showing when recurrence compatibility is exact, unavailable, or approximate, so checkpoint trace rows cannot silently claim sketched recurrence correctness. Evidence: `src/fhe_native_mamba3/sketch_recurrence_claims.py`, `Stage2SketchSweepRow.sketch_recurrence_claim`, `CheckpointSourceSketchTrace.to_json_dict()["sketch_recurrence_claim"]`, and tests in `tests/test_sketch_recurrence_claims.py`, `tests/test_stage2_sketch_sweep.py`, and `tests/test_checkpoint_sketch_trace.py`. Result: scalar/rank-scalar measured compatibility can be `exact` or `approximate`, while rank-state rows are explicitly `unavailable` and `readout_error_only=true`. |
| PBI-S2-008 | Stage 2 | Done | PBI-S1-007, PBI-S2-004 | Build lazy-bootstrap scheduling simulations from measured Stage 1 pack costs and Stage 2 sketch tradeoffs. Evidence: `scripts/build_lazy_bootstrap_report.py`, tests in `tests/test_lazy_bootstrap.py` / `tests/test_lazy_bootstrap_script.py`, refreshed Stage 1 comparison report `runs/stage1-comparison-report-fideslib-v0350.json`, and lazy-bootstrap report `runs/stage2-s008-lazy-bootstrap-report-fideslib-v0350.json`. Result: with FIDESlib Stage 1 bootstrap latency `0.0216s`, the simulation recommends pack/sketch `16/16`, reports `11` scheduled bootstraps/token for that row, and estimates `0.0149 sec/token` amortized bootstrap time. This is simulation-only; no encrypted Stage 2 execution or full-model correctness is claimed. |
| PBI-S2-009 | Stage 2 | Open | PBI-S0-010, PBI-S2-004 | Run range-aware calibration or LoRA only where profile/trace artifacts show sketch or polynomial-range failure. Acceptance requires before/after profile JSON, seed-sweep JSON, and unchanged correctness smoke results. |
| PBI-S2-010 | Stage 2 | Done | PBI-S2-003 | Implement a plaintext/client-side decode artifact matrix for the current encrypted-hidden baseline. Acceptance requires prompts, logits/top1-gap JSON, and clear accounting of where decryption occurs. Evidence: `src/fhe_native_mamba3/client_decode_report.py`, `scripts/build_client_decode_report.py`, tests in `tests/test_client_decode_report.py` / `tests/test_client_decode_report_script.py`, and artifact `runs/stage2-s010-client-decode-report.json`. Result: three real-checkpoint client-side decode artifacts over prompts `[1,2,3,4]`, `[7,7,7,7]`, and `[1]` report selected tokens `330`, `30491`, and `6599`; min top1/top2 gap `1.2569`, output payload width `50280`, total client decrypt count `3`, and `encrypted_argmax=false`. |
| PBI-S2-011 | Stage 2 | Done | PBI-S2-003 | Prototype a toy encrypted CutMax/argmax path on small vocab sizes. Evidence: `src/fhe_native_mamba3/toy_cutmax.py`, `scripts/run_stage2_toy_cutmax_smoke.py`, tests in `tests/test_toy_cutmax.py`, tracking artifact `runs/stage2-s011-toy-cutmax-tracking.json`, and high/OpenFHE job `10220` artifact `runs/stage2-s011-toy-cutmax-openfhe.json`. Result: toy vocab size `4`, encrypted OpenFHE path passed with `selected_argmax=0`, `ct_ct_mul=9`, `rotations=3`, `eval_seconds=1.68`, SLURM elapsed `5s`, MaxRSS `717800K`; it is not full-vocab generation evidence. |
| PBI-S2-012 | Stage 2 | Done | PBI-S2-001 | Add a reusable checkpoint sketch matrix runner that sweeps layers, prompt sets, rank-selection strategies, sketch sizes, and SRHT seeds without claiming encrypted correctness. Evidence: `src/fhe_native_mamba3/checkpoint_sketch_matrix.py`, `scripts/run_checkpoint_sketch_matrix.py`, `slurm/checkpoint_sketch_matrix.sbatch`, and `tests/test_checkpoint_sketch_matrix.py`. |
| PBI-S2-013 | Stage 2 | Done | PBI-S2-004 | Produce a compact sketch evidence report from accepted matrix artifacts. Acceptance requires a JSON/Markdown report with pass-rate by layer/prompt/rank strategy, recommended sketch size, worst product-norm error, and explicit recurrence-type caveats. Evidence: `src/fhe_native_mamba3/sketch_evidence_report.py`, `scripts/build_stage2_sketch_evidence_report.py`, tests in `tests/test_sketch_evidence_report.py` / `tests/test_sketch_evidence_report_script.py`, and artifact `runs/stage2-s013-sketch-evidence-report.json`. Result: checkpoint sketch matrix `10135` condenses to 12 rows over layers 0/12/23, prompts short/repeat, rank strategies first/stride; recommended sketch size is `16` for 11/12 rows and `8` for 1/12, worst product-norm error `0.2472`, with rank-state recurrence caveats preserved. |
| PBI-S2-014 | Stage 2 | Done | PBI-S2-005, PBI-S2-013 | Expand learned sketch baselines from a single trace to the checkpoint sketch matrix. Acceptance: run PCA/SVD learned rows over the same layer/prompt/rank-strategy grid used by PBI-S2-013, emit a compact learned-vs-SRHT report, and keep metadata explicit that projection training is plaintext/offline and data-dependent. Evidence: `src/fhe_native_mamba3/checkpoint_learned_sketch_matrix.py`, `src/fhe_native_mamba3/learned_sketch_report.py`, scripts `run_checkpoint_learned_sketch_matrix.py` and `build_stage2_learned_sketch_report.py`, tests in `tests/test_checkpoint_learned_sketch_matrix.py` / `tests/test_learned_sketch_report.py`, matrix artifact `runs/stage2-s014-learned-sketch-matrix-v0387.json`, and report artifact `runs/stage2-s014-learned-sketch-report-v0388.json`. Result: over the PBI-S2-013 grid (layers `0/12/23`, prompts `short/repeat`, rank strategies `first:8` and `stride:8:64`), learned PCA/SVD recommends sketch size `4` for `12/12` rows with worst pairnorm L2 error `3.06e-02`; matched SRHT recommends size `16` for `11/12` rows and size `8` for `1/12`. Scope remains plaintext/offline and data-dependent; no encrypted execution or perplexity claim. |
| PBI-S2-015 | Stage 2 | Done | PBI-S2-009, PBI-S2-014, PBI-S0-010 | Add a range-calibration versus LoRA decision gate before launching GPU fine-tuning. Acceptance: consume the repeat-prompt scale plan, learned-sketch report, and unchanged full-visible correctness smoke; emit a JSON recommendation that either launches LoRA or defers it with explicit thresholds. Evidence: `src/fhe_native_mamba3/stage2_range_lora_decision.py`, `scripts/build_stage2_range_lora_decision.py`, tests in `tests/test_stage2_range_lora_decision.py` / `tests/test_stage2_range_lora_decision_script.py`, and artifact `runs/stage2-s015-range-lora-decision-v0391.json`. Result: the report sees range calibration is needed (`activation_tuning_layer_count=21`, state/output scaling needed in `24/24` layers) but existing deterministic calibration evidence passes (`max_abs_error=1.05e-02` under the `8e-2` gate), learned PCA/SVD sketch evidence is under threshold (`worst_learned_recommended_pairnorm_l2_error=3.06e-02` under `5e-2`), and the recommendation is `defer_lora_use_deterministic_calibration`. No LoRA training is executed or claimed. |
| PBI-S2-016 | Stage 2 | Done | PBI-S2-009, PBI-S1-045 | Diagnose direct truncated-SVD low-rank replacement for the dense rank/gate/output projections before investing in a native low-rank encrypted kernel. Acceptance: implement an offline payload sweep that distinguishes compressed-rank passes from full-rank identity passes, run it on both the LoRA-merged synthetic Mamba-130M-shaped payload and a real `state-spaces/mamba-130m-hf` layer-0 payload, and validate artifacts. Evidence: `src/fhe_native_mamba3/stage2_low_rank_payload_sweep.py`, `scripts/run_stage2_low_rank_payload_sweep.py`, tests in `tests/test_stage2_low_rank_payload_sweep.py`, synthetic artifact `runs/stage2-s019-low-rank-payload-sweep-mamba130m-v03150.json`, real-checkpoint payload `runs/stage2-s020-real-mamba130m-rank-gate-layer0-v03149.bin`, and real-checkpoint sweep `runs/stage2-s021-real-mamba130m-low-rank-sweep-layer0-v03150.json`. Result: the LoRA-merged synthetic payload has a compressed rank-8 pass, but the real Mamba-130M layer-0 payload has no compressed pass under `output_delta_atol=5e-2`; only full rank `768` passes. This makes direct post-hoc low-rank truncation a poor Stage 1 speed path for real weights unless paired with LoRA/distillation or a different factorized architecture. |
| PBI-S2-017 | Stage 2 | Done | PBI-S2-016, PBI-S1-045 | Diagnose whether coefficient-threshold pruning of public dense projection weights can reduce native BSGS plaintext-mask work before changing the C++ coefficient floor. Acceptance: implement an offline payload sweep over conv/gate/output/all projection matrices, report reference drift plus estimated ct-pt/mask reductions, run it on a real Mamba-130M layer-0 payload, and keep the artifact explicitly plaintext/offline. Evidence: `src/fhe_native_mamba3/stage2_projection_prune_sweep.py`, `scripts/run_stage2_projection_prune_sweep.py`, tests in `tests/test_stage2_projection_prune_sweep.py`, and artifact `runs/stage2-s023-real-mamba130m-projection-prune-sweep-layer0-v03151.json`. Result: correctness remains within `output_delta_atol=5e-2` for conv/all thresholds up to `1e-4` and gate/output thresholds up to `1e-3`, but every useful compressed row has `ct_pt_reduction=0` and `projection_rotation_reduction=0`; the aggregate `passed=false`. Raising the native coefficient floor alone is therefore not a credible dense-projection runtime fix for this real layer-0 payload. |
| PBI-S2-018 | Stage 2 | Done | PBI-S2-017, PBI-S1-045 | Diagnose whether whole BSGS diagonal/mask pruning can remove native ct-pt multiplies with acceptable payload-reference drift. Acceptance: implement a keep-fraction sweep over conv/gate/output/all projection masks, score masks by l2/mean/max magnitude, require a configurable minimum ct-pt reduction fraction, run on real Mamba-130M layer 0, and keep claims plaintext/offline. Evidence: `src/fhe_native_mamba3/stage2_bsgs_mask_prune_sweep.py`, `scripts/run_stage2_bsgs_mask_prune_sweep.py`, tests in `tests/test_stage2_bsgs_mask_prune_sweep.py`, and artifact `runs/stage2-s024-real-mamba130m-bsgs-mask-prune-sweep-layer0-v03152.json`. Result: with `min_ct_pt_reduction_fraction=0.05`, no useful row passes. The best correct conv row keeps `0.97` of masks with `mean_abs` scoring and reduces `69` masks (`2.996%`) at output delta `2.27e-2`; the best correct gate row keeps `0.98` with `l2` scoring and reduces `46` masks (`1.997%`) at output delta `2.95e-2`; output/all have no useful reduction. Whole-mask pruning without training is therefore also not a credible dense-projection runtime fix. |
| PBI-S2-019 | Stage 2 | Done | PBI-S2-016, PBI-S2-017, PBI-S2-018 | Build a dense-projection decision artifact that combines post-hoc low-rank, coefficient-floor pruning, and whole-mask pruning evidence into one branch decision. Acceptance: consume the three JSON artifacts, emit a recommendation with no encrypted/full-model claim, and make the recommendation fail closed to training-time factorization or group-sparse LoRA when no post-hoc path clears the useful reduction threshold. Evidence: `src/fhe_native_mamba3/stage2_dense_projection_decision.py`, `scripts/build_stage2_dense_projection_decision.py`, tests in `tests/test_stage2_dense_projection_decision.py`, and artifact `runs/stage2-s025-dense-projection-decision-real-mamba130m-layer0-v03153.json`. Result: `recommended_action=train_factorized_or_group_sparse_projection`, `credible_posthoc_path_found=false`, and the best correct BSGS-mask row only reduces `2.996%` of ct-pt masks (`target=conv`), below the `5%` useful threshold. |
| PBI-S2-020 | Stage 2 | Done | PBI-S2-019 | Add a plaintext group-sparse LoRA smoke that tries to create BSGS-mask structure during training rather than pruning it after the fact. Acceptance: train LoRA adapters on the rank/gate projection boundary with a group-lasso penalty over low-score BSGS masks, report before/after task/range/mask metrics, replay the merged payload through the BSGS-mask sweep, and keep claims plaintext/offline. Evidence: `src/fhe_native_mamba3/stage2_group_sparse_lora_smoke.py`, `scripts/run_stage2_group_sparse_lora_smoke.py`, tests in `tests/test_stage2_group_sparse_lora_smoke.py`, artifacts `runs/stage2-s026-real-mamba130m-group-sparse-lora-smoke-layer0-v03154.json` and `runs/stage2-s027-real-mamba130m-group-sparse-lora-strong-layer0-v03154.json`. Result: the lighter run reduced mask group loss `0.0629 -> 0.0353` and range excess `1.1467 -> 0`, but did not clear the `5%` useful mask-reduction threshold. The stronger run (`rank=16`, `mask_weight=10`, `penalized_mask_fraction=0.1`) reduced mask group loss `0.0642 -> 0.0312` and produced useful merged mask-sweep rows: conv `9.99%` ct-pt reduction at output delta `2.99e-2`, gate `7.47%` ct-pt reduction at output delta `4.51e-2`. This is positive evidence that training-time structure can create native mask savings where post-hoc pruning could not. |
| PBI-S2-021 | Stage 2 | Done | PBI-S2-020 | Build an aggregate report for group-sparse LoRA smoke artifacts so layer and hyperparameter expansion can be judged by a stable criterion. Acceptance: consume multiple smoke JSONs, report best useful target/reduction/delta, recommend expansion only when at least one artifact clears the useful ct-pt reduction threshold, and emit a validator-clean artifact. Evidence: `src/fhe_native_mamba3/stage2_group_sparse_lora_report.py`, `scripts/build_stage2_group_sparse_lora_report.py`, tests in `tests/test_stage2_group_sparse_lora_report.py`, and artifact `runs/stage2-s028-group-sparse-lora-report-real-mamba130m-layer0-v03155.json`. Result: report passes and recommends `expand_group_sparse_lora_to_more_layers`; best artifact is `runs/stage2-s027-real-mamba130m-group-sparse-lora-strong-layer0-v03154.json`, best target `conv`, best useful ct-pt reduction fraction `9.99%`, output delta `2.99e-2`. Follow-up fix in `v0.3.156`: report rows now recompute usefulness from stored merged-sweep rows under the report-time threshold instead of inheriting the smoke artifact's baked threshold, so sensitivity reports are not silently stale. |
| PBI-S2-022 | Stage 2 | Done | PBI-S2-021 | Expand the group-sparse LoRA smoke from layer 0 to representative mid/final real Mamba-130M layers. Acceptance: export layer 12 and 23 rank/gate payloads, run the same strong group-sparse LoRA setting, aggregate layer 0/12/23 in the report, and record whether useful mask savings reproduce across layers. Evidence: payload exports `runs/stage2-s029-real-mamba130m-rank-gate-layer12-v03155.json` / `runs/stage2-s030-real-mamba130m-rank-gate-layer23-v03155.json`, smoke artifacts `runs/stage2-s031-real-mamba130m-group-sparse-lora-strong-layer12-v03155.json` / `runs/stage2-s032-real-mamba130m-group-sparse-lora-strong-layer23-v03155.json`, the default aggregate report `runs/stage2-s033-group-sparse-lora-report-real-mamba130m-layers0-12-23-v03155.json`, stronger layer-12 artifact `runs/stage2-s034-real-mamba130m-group-sparse-lora-stronger-layer12-v03155.json`, and sensitivity report `runs/stage2-s035-group-sparse-lora-report-real-mamba130m-layers0-12-23-threshold049-v03156.json`. Result: under the default `5%` useful threshold, layer 0 remains the only clear useful pass (`conv` `9.99%` ct-pt reduction), layer 12 is borderline (`conv` `4.993%` reduction at output delta `3.72e-2`), and layer 23 is weak (`conv` `0.999%`). Under a sensitivity threshold of `4.9%`, layer 12 becomes useful while layer 23 remains weak. Training reduces mask group loss on all three layers, but native mask savings are layer-dependent under these hyperparameter settings. |
| PBI-OPS-001 | DevEx | Done | none | Add fast/slow test profiles so low-risk edits run a short local/remote gate while OpenFHE, SLURM, and full pre-commit checks remain available as explicit slow gates. Evidence: `scripts/run_fast_checks.sh`, `scripts/run_checks.sh`, `scripts/remote_checks.sh`, and `docs/testing.md`; full checks avoid duplicate pre-commit execution unless `RUN_PRECOMMIT=1` is requested. |
| PBI-OPS-002 | DevEx | Done | none | Maintain an artifact ledger that maps high/SLURM job IDs to PBI IDs, JSON paths, git commits, and pass/fail status. Evidence: `docs/artifact_ledger.md`, `scripts/update_artifact_ledger.py`, and tests in `tests/test_artifact_ledger_update_script.py`. The updater consumes `ledger_rows` emitted by safe-campaign collection artifacts, dry-runs by default, appends with `--write`, dedupes exact rows, and fails on same-job/artifact conflicts instead of silently overwriting richer curated ledger entries. |
| PBI-OPS-003 | DevEx | Done | PBI-OPS-002 | Export backlog PBIs to GitHub issues when repository permissions are available; Project board item movement is out of scope for this slice. Acceptance requires issue titles, dependencies, and status labels generated from `docs/backlog.md` without hand-copying. Evidence: `src/fhe_native_mamba3/backlog_issues.py`, `scripts/sync_backlog_issues.py`, tests in `tests/test_backlog_issues.py` / `tests/test_backlog_issue_sync_script.py`, dry-run artifact `runs/pbi-ops-003-issue-sync-plan-v0390.json`, and refreshed dry-run artifact `runs/pbi-ops-003-issue-sync-plan-v03127.json`. Result: the latest dry run parses `75` backlog PBIs, tracks `17` existing GitHub issues by PBI ID, and emits deterministic action counts `close=1`, `update=2`, `noop=14`, `create=0`, `reopen=0`; it does not apply changes to GitHub. |
| PBI-OPS-004 | DevEx | Done | PBI-OPS-002 | Add a safe parallel SLURM campaign runner for low/medium-risk evidence collection. Evidence: `scripts/submit_safe_slurm_campaign.py` submits/dry-runs only whitelisted small jobs, assigns unique `RUN_NAME`s, records job IDs, and emits manifest/ledger-row templates; `scripts/collect_safe_slurm_campaign.py` validates completed artifacts, emits ledger-row candidates, and now supports `--pull-missing --remote high --remote-dir ~/cipher/fhe-native-mamba3` with `--pull-dry-run` for safe validation. Covered by `tests/test_safe_slurm_campaign_script.py` and `tests/test_safe_slurm_campaign_collect_script.py`, exercised by high jobs `10157`-`10163`. High-memory OpenFHE full-chain jobs remain explicitly excluded from the safe campaign manifest. |
| PBI-OPS-005 | DevEx | Done | PBI-OPS-002 | Add a single heavy-SLURM-job collection helper for jobs that should not go through the safe campaign runner. Acceptance: poll `sacct`, optionally pull the expected artifact with `rsync`, validate artifact schema when present, and emit a ledger-row candidate without failing on still-running jobs unless requested. Evidence: `scripts/collect_slurm_job_artifact.py`, tests in `tests/test_collect_slurm_job_artifact_script.py`, and live running collection artifact `runs/stage1-s041-running-collection-v0392.json` for job `10300`. Result: the helper reports `root_state=RUNNING`, `collection_complete=false`, and `artifact_exists=false` for the in-flight PBI-S1-041 job, so future polling can collect the final artifact without manual path reconstruction. |
| PBI-OPS-006 | DevEx | Done | PBI-OPS-002, PBI-S2-022 | Tighten rank/gate payload export artifacts so future layer-expansion runs are validator-clean before they feed Stage 2 LoRA and pruning sweeps. Acceptance: add explicit `config.input_mode`, zero operation/rotation counts, and a human-readable non-encrypted claim to `stage1-rank-gate-payload-export`; cover the script artifact with validator assertions. Evidence: `scripts/export_stage1_rank_gate_payload.py` and `tests/test_stage1_rank_gate_payload.py`. Result: payload exports remain plaintext/offline but no longer emit schema warnings for missing input mode, operation counts, rotations, or measurement-scope claim. |

## Recent Diagnostic Notes

- `v0.3.116` adds native FIDESlib `ckks_levels` telemetry plus
  `scripts/build_stage1_ckks_level_report.py`. Tiny zero-state job `10429`
  validates the schema and reports max consumed level `21` under depth `48`
  with margin `27`; because that artifact is zero-state, the next PBI-S1-045
  evidence should be nonzero-state telemetry before choosing a bootstrap
  insertion point.
- `v0.3.117` adds decrypt-failure JSON emission around the same telemetry.
  Nonzero high-degree job `10433` fails during CKKS decode but still emits
  `ckks_levels`; max consumed level is `26` under depth `48`, so the immediate
  blocker is approximation/noise or scale precision, not raw level exhaustion.
  Follow-up job `10435` passes the same tiny nonzero high-degree path at
  `scaling_mod_size=45`, so scale tuning is now a concrete candidate before
  adding bootstrap into the FIDESlib one-layer kernel.
- Mamba-130M-shaped nonzero high-degree validation needed more precision:
  `scaling_mod_size=45` still failed at decrypt in job `10438`, while
  `scaling_mod_size=50` passed in job `10441` with `ct_ct_mul=31`,
  `rotations=1036`, eval `924.32s`, peak RSS `81.45 GiB`, and max consumed
  level `27/48`. This closes the immediate nonzero-state one-layer correctness
  blocker for the synthetic Mamba-130M-shaped FIDESlib slice; multi-layer
  chaining and bootstrap scheduling remain open.
- `v0.3.118` adds a repeated encrypted recurrent-state chain smoke to the
  native FIDESlib rank/gate kernel. Tiny high/B200 job `10483` passed with
  `CHAIN_STEPS=2`, nonzero previous state, `max_abs_error=0`,
  `diagnostic_max_abs_error=3.10e-05`, eval `24.90s`, peak RSS `14.27 GiB`,
  and max consumed level `27/48`. This validates encrypted recurrent-state
  handoff inside the Stage 1 slice; it does not claim model-layer-to-layer
  handoff or full-model success.
- `v0.3.119` adds chain-scaling reports that compare `CHAIN_STEPS=1` and
  `CHAIN_STEPS=2` artifacts to separate setup/key/context costs from recurrent
  eval increments. Tiny chain scaling (`10489` vs `10483`) reports an extra
  encrypted recurrent step at `+0.40s`, `+7` rotations, `+14` ct-pt muls, and
  `+3` ct-ct muls. Small96 chain scaling (`10490` vs `10488`) reports
  `+10.58s`, `+37` rotations, `+160` ct-pt muls, and `+3` ct-ct muls. Both
  pass with no bootstrap; Mamba-130M-shape chain2 remains in flight as job
  `10487`.
- `v0.3.121` adds a recurrent-chain bootstrap schedule report. The accepted
  tiny and small96 chain1/chain2 pairs both show `+1` consumed CKKS level per
  added recurrent update. Projecting to `24` recurrent updates at depth `48`
  and min level `2` requires one prospective bootstrap before
  `recurrent-step-22`. This is a schedule derived from telemetry, not an
  executed bootstrap run.
- `v0.3.148` adds exact FIDESlib reductions for the Mamba-130M-shaped
  `dt_hidden` and `dt_rank` projections on the LoRA-merged layer-0 payload.
  Jobs `10603` and `10607` both pass with final `max_abs_error=0`. The combined
  reduction drops one-layer eval from the B/C-reduced baseline `753.84s` to
  `493.96s`, ct-pt multiplications from `10173` to `7199`, and required
  application rotation keys from `200` to `214`; the remaining top phases are
  conv/gate/output dense projections at about `159s`, `150s`, and `156s`.

## Dependency Map

- Real encrypted chain work: PBI-S0-008 -> PBI-S0-009 is complete at the scoped Stage 0 objective: measured smaller proxies and closeout/handoff, not full 24-layer encrypted success.
- Stage 1 grouped checkpoint path: PBI-S1-009 -> PBI-S1-010 -> PBI-S1-011 ->
  PBI-S1-012 -> PBI-S1-013 -> PBI-S1-014 -> PBI-S1-015 -> PBI-S1-016 are
  complete through PBI-S1-021. Bootstrap evidence is no longer the blocker.
  PBI-S1-020 records a composite-rotation diagnostic fallback; PBI-S1-021
  records the preferred state-major rank-pack-first planner. PBI-S1-022 adds
  the first executable state-major toy kernel; PBI-S1-023 records fixed-schedule
  tracking BSGS projection traces; PBI-S1-024 proves true slot-semantics BSGS
  projection under OpenFHE on a tiny state-major kernel; PBI-S1-025 refreshes
  the full-shape planner to `133` application rotation keys / `37.50 GiB`
  estimated total key memory; PBI-S1-026 validates the executable state-major
  slot-BSGS graph at Mamba-130M-like full shape in tracking mode; PBI-S1-027
  connects the same recurrence/readout/out-projection tail to a checkpoint
  layer with explicit adapter-vs-kernel error split; PBI-S1-028 moves
  `rank_input` and `gate` into the same state-major graph; PBI-S1-029 moves
  dynamic `B/C` into state-major slots; PBI-S1-030 moves state-rank decay into
  state-major slots; PBI-S1-031 validates that computed decay affects a nonzero
  recurrent state; PBI-S1-033 adds the first two-layer tracking chain for
  model-layout residual ciphertext handoff; PBI-S1-034 adds a tracking
  polynomial-degree sweep for the OpenFHE retry loop; PBI-S1-032 closes the
  tiny OpenFHE bridge at degree 11; PBI-S1-035 recovers a mixed-degree
  OpenFHE path with rank degree 15 and gate degree 9; PBI-S1-036 runs that
  path through a tiny two-layer OpenFHE chain; PBI-S1-037 adds a guarded
  shape scale sweep before larger checkpoint execution; PBI-S1-038 executes
  the first allowed small-shape OpenFHE bridge; PBI-S1-039 moves the same path
  to the guarded medium shape; PBI-S1-040 proves the Mamba-130M-shape
  OpenFHE setup/keygen path fits under the explicit guard; PBI-S1-041 proves
  the bounded Mamba-130M one-layer OpenFHE eval; PBI-S1-042 selects the next
  scaling direction; PBI-S1-043 proves the target 163-key FIDESlib
  rotation/key-memory primitive path; PBI-S1-044 proves the target one-layer
  op mix is fast enough to justify continuing. PBI-S1-045 is in progress:
  the Python checkpoint adapter can now export a fixed tail payload, native C++
  can parse/evaluate that tail, and FIDESlib encrypted tail jobs have passed for
  both tiny and Mamba-130M-shaped payloads. Rank/gate pre-recurrence payload
  parity is also available natively; encrypted FIDESlib rank/gate projection
  passes through Mamba-130M shape; and the encrypted native slice now covers
  polynomial SiLU, skip update, dynamic B/C, and state-major B/C
  broadcast through Mamba-130M shape. The encrypted native slice now also
  covers state-major decay, recurrence, readout, and output projection, with a
  tiny repeated recurrent-state chain smoke validating ciphertext state reuse.
  The native recurrent bootstrap hook has also executed one scheduled bootstrap
  in a tiny `CHAIN_STEPS=3` run, and the resulting artifact now satisfies the
  common validation schema without warnings.
  Mamba-130M-shaped chain2 scaling is now recorded: the extra recurrent update
  costs `212.50s`, `102` rotations, `2304` ct-pt muls, and `3` ct-ct muls,
  with max consumed level `28/48`. The remaining blockers are model-layout
  layer-to-layer handoff and executing the derived recurrent bootstrap schedule
  at a larger projected boundary before any full 24-layer claim.
  The latest phase-guided one-layer optimization has also removed dynamic B/C,
  `dt_hidden`, and `dt_rank` as major runtime bottlenecks on the Mamba-130M
  layer-0 payload; the active exact-path bottleneck is now the dense
  conv/gate/output projection trio.
- Sketch/range evidence: PBI-S2-001 -> PBI-S2-012 -> PBI-S2-004 -> PBI-S2-013 is complete; PBI-S2-005 adds the first learned/data-dependent sketch baseline; PBI-S2-014 expands it to the matrix report; PBI-S2-015 adds the current range/LoRA decision gate. PBI-S2-004 and PBI-S0-010 continue to feed future PBI-S2-009 work if later encrypted chains expose a calibration failure.
- Encrypted sketch execution: PBI-S2-006 and PBI-S2-007 are complete; the learned/data-dependent sketch branch is complete through PBI-S2-014. PBI-S2-015 currently recommends deferring LoRA until a later chain artifact fails the existing deterministic calibration gate.
- Decoding branch: PBI-S2-003 is complete at the current scope. PBI-S2-010 records client-side decode accounting, and PBI-S2-011 records a toy encrypted CutMax/OpenFHE path. Full-vocab encrypted generation is not claimed.
- Development operations: PBI-OPS-001 through PBI-OPS-006 are done at their current scope. PBI-OPS-003 provides deterministic GitHub Issue sync plans from the canonical backlog, PBI-OPS-005 collects single heavy SLURM jobs outside the safe campaign runner, and PBI-OPS-006 keeps rank/gate payload exports validator-clean; GitHub Project board automation is not yet claimed.
- `v0.3.128` adds the first model-layout handoff payload export for the
  native/FIDESlib line. `runs/stage1-s054-tiny-rank-gate-chain-payload-v03128.json`
  contains two sequential tiny checkpoint payloads; layer 1's residual input
  follows layer 0's polynomial model-layout output with max handoff difference
  `1.19e-07`. This is a payload/export contract only; the next executable
  blocker is native multi-payload evaluation that keeps the layer-0 output
  ciphertext alive as the layer-1 residual input.
- `v0.3.129` adds a FIDESlib-free native `--input-chain` evaluator for rank/gate
  payload handoff contracts. It validates the same two-payload artifact at
  `max_abs_error=6.45e-07` and `model_layout_handoff_max_abs_error=1.19e-07`
  without GPU/FIDESlib, keeping `chain_steps` separate from layer payload count.
- `v0.3.130` wires `--input-chain` into the encrypted FIDESlib rank/gate
  executable and SLURM path. The executable now reads a same-shape payload
  sequence, unions rotation keys, and feeds payload N's encrypted
  `output_model` ciphertext as payload N+1's residual while keeping each
  layer's pre-recurrence tensors payload/reference-driven. Remote FIDESlib
  compile on `high` passes; the next artifact should be a tiny two-payload
  encrypted model-layout handoff run.
- Job `10497` closes that tiny handoff smoke: `runs/stage1-s055-tiny-model-handoff-v03130.json`
  passes with `payload_count=2`, `model_layout_ciphertext_handoff=true`,
  final `max_abs_error=0`, handoff reference error `5.43e-08`, `110`
  rotations, `150` ct-pt multiplies, `60` ct-ct multiplies, eval `42.60s`,
  and peak RSS `13.64 GiB`. This is still tiny-shape evidence; the next
  Stage 1 scaling step is small96 or Mamba-130M-shaped model-layout handoff.
- Job `10498` moves the same encrypted model-layout handoff to the small96
  checkpoint shape: `runs/stage1-s056-small96-model-handoff-v03131.json`
  passes with `d_model=64`, `mimo_rank=96`, `d_state=4`, `payload_count=2`,
  `model_layout_ciphertext_handoff=true`, final `max_abs_error=0`, diagnostic
  max error `2.47e-07`, handoff reference error `4.65e-10`, `56` application
  rotation keys, `544` rotations, `1782` ct-pt multiplies, `60` ct-ct
  multiplies, eval `142.59s`, peak RSS `30.09 GiB`, and SLURM elapsed
  `00:03:35`. This closes the immediate tiny-only concern for model-layout
  handoff; the next scaling step is Mamba-130M-shaped two-payload handoff or a
  cheaper layer-boundary profiling split before launching that heavier job.
- `v0.3.133` adds a model-layout handoff scaling report. Comparing tiny job
  `10497` and small96 job `10498`, `ct_ct_mul` stays flat at `60` for two
  payloads while rotations grow `110 -> 544`, ct-pt multiplications grow
  `150 -> 1782`, eval grows `42.60s -> 142.59s`, and peak RSS grows
  `13.64 -> 30.09 GiB`. This supports the current diagnosis that the next
  Mamba-130M-shaped two-layer attempt is projection/rotation dominated rather
  than recurrence-multiplication dominated.
- Job `10500` closes the Mamba-130M-shaped two-payload encrypted handoff
  attempt: `runs/stage1-s057-mamba130m-model-handoff-v03133.json` passes with
  `d_model=768`, `mimo_rank=1536`, `d_state=16`, `payload_count=2`,
  `model_layout_ciphertext_handoff=true`, final polynomial `max_abs_error=0`,
  handoff reference error `4.66e-10`, payload-chain reference error
  `2.46e-06`, `189` application rotation keys, `2072` rotations, `26422`
  ct-pt multiplies, `60` ct-ct multiplies, eval `2114.35s`, peak RSS
  `77.71 GiB`, and SLURM elapsed `00:38:52`. The exact-reference diagnostic
  error is still large (`4.50`), so the evidence is correctness of the
  encrypted polynomial execution and model-layout ciphertext handoff, not
  language-model quality preservation. The next bottleneck is reducing
  projection/rotation cost and deciding whether to pursue 24-layer synthetic
  polynomial execution or first add range calibration / lower-degree adapters.
- `v0.3.135` refreshes the handoff scaling report to carry exact-reference
  diagnostics. The Mamba-130M-shaped row has
  `output_model_poly_vs_exact_max_abs_error=9.65e-04` over two reference steps
  even though the broader diagnostic max is `4.50`; this keeps the artifact
  useful for engineering regression checks while still avoiding any claim about
  language-model quality.
- `v0.3.136`/`v0.3.137` start PBI-S2-009 with an actual LoRA range-tuning
  smoke over rank/gate payload binaries. The first CUDA attempt exposed and
  fixed a real adapter placement bug: newly created LoRA matrices now inherit
  the wrapped base linear layer's device/dtype. After the fix, B200 job
  `10569` trains a rank-8 adapter for 200 steps on the Mamba-130M-shaped
  layer-0 payload and reduces `gate_pre_max_abs` from `7.33` to `6.51`, range
  loss from `0.881` to `0.131`, and total loss from `0.881` to `0.146`. This is
  still plaintext projection-boundary tuning; the next PBI-S2-009 slices are
  a seed/step sweep and then re-exporting a LoRA-merged payload for encrypted
  comparison.
- `v0.3.138` runs that seed/step/range-weight sweep on B200. Job `10574`
  covers 27 rows over seeds `0/1/2`, steps `200/500/1000`, and range weights
  `1/2/4`; the best row is seed `2`, `500` steps, weight `2`, reducing
  Mamba-130M layer-0 `gate_pre_max_abs` from `7.3268` to `5.9955` with zero
  remaining range loss and task MSE `2.00e-03`. The next slice is now concrete:
  merge the selected LoRA weights back into the public rank/gate payload and
  replay the encrypted polynomial path against the merged references.
- `v0.3.139` implements that merge path. Job `10577` trains the selected
  rank-8 LoRA setting on B200 and writes
  `runs/stage2-s018-lora-merged-payload-mamba130m-v03139.bin`; the merged
  payload validates with `gate_weight_delta_max_abs=9.07e-02`,
  `reference_gate_pre_delta_max_abs=6.94`, post-merge
  `gate_poly_vs_exact_max_abs_error=5.06e-02`, and
  `output_model_poly_vs_original_exact_max_abs_error=9.90e-04`. This still
  does not claim encrypted execution; the encrypted replay is the next check.
- `v0.3.141` closes the LoRA merged-payload encrypted replay loop. Job `10580`
  runs the merged Mamba-130M-shaped layer-0 payload through the native FIDESlib
  encrypted path and passes with `max_abs_error=0`, `diagnostic_max_abs_error=
  0.726`, `output_model_poly_vs_exact_max_abs_error=9.90e-04`, `189`
  application rotation keys, `1036` rotations, `13211` ct-pt multiplies, `30`
  ct-ct multiplies, setup `173.08s`, rotate-keygen `69.32s`, context load
  `100.02s`, eval `968.84s`, and peak RSS `77.67 GiB`. LoRA successfully
  clamps the gate preactivation to the target range while preserving the
  encrypted polynomial reference, but it does not materially change the Stage 1
  runtime bottleneck; dense projection cost remains the next target.
- `v0.3.142`-`v0.3.146` add native FIDESlib phase telemetry and a targeted B/C
  projection optimization. Baseline job `10587` passes the LoRA-merged
  Mamba-130M-shaped layer-0 payload with `eval_seconds=994.84`, and the phase
  report shows dense ct-pt projections dominate: output `182.19s`, conv
  `158.32s`, gate `155.15s`, dt-rank `122.05s`, dynamic C `120.30s`,
  dt-hidden `120.22s`, and dynamic B `120.09s`. Baby-rotation caching job
  `10588` reduces rotations `1036 -> 879` but is slower (`1036.86s`), so it is
  not the main path. Rank-reduce B/C job `10590` passes with `max_abs_error=0`,
  reduces ct-pt multiplications `13211 -> 10173`, collapses dynamic B/C
  projections to `1.19s` / `1.18s`, and lowers eval to `753.84s` (`1.32x`).
  The remaining Stage 1 bottleneck is now conv/gate/output/dt dense projection,
  not B/C, recurrence, or polynomial nonlinearities.
- `v0.3.150` adds a direct truncated-SVD low-rank diagnostic for rank/gate/output
  payload projections. It confirms that the LoRA-merged synthetic Mamba-130M-
  shaped payload has a compressed rank-8 pass, but the real
  `state-spaces/mamba-130m-hf` layer-0 payload has no compressed pass under
  `output_delta_atol=5e-2`; only full rank `768` passes. A small real-checkpoint
  LoRA range sweep still succeeds as a range-control tool, reducing
  `gate_pre_max_abs` from `7.1291` to `5.6763` with zero remaining range excess
  and task MSE `1.47e-03`, but direct post-hoc SVD is not a credible projection
  runtime fix without distillation or architecture changes.

## Near-Term Parallel Slices

- Mainline A: continue PBI-S1-045 by adding model-layout layer-to-layer handoff
  execution on top of the B/C rank-reduce FIDESlib kernel, then reduce the
  remaining conv/gate/output/dt dense projection cost before attempting broader
  24-layer scheduling.
- Stage 2 B: PBI-S2-009 now has a complete LoRA range-tuning -> payload merge
  -> encrypted replay slice for Mamba-130M layer 0, plus a real-checkpoint layer-0
  range sweep. Continue LoRA only for range control or distillation; direct
  post-hoc SVD low-rank replacement should not be prioritized as the Stage 1
  dense-projection runtime fix.
- Low-risk reporting C: run `scripts/sync_backlog_issues.py` after backlog
  edits to keep GitHub Issue plans in sync; direct Project board automation is
  future DevEx work, not claimed in this line. Use
  `scripts/collect_slurm_job_artifact.py` for in-flight heavy jobs such as
  PBI-S1-041/job `10300`.

## Stale Or Obsolete Notes

- README version `0.2.100` was stale and has been updated through `0.3.15`.
- Any backlog item phrased as "full OpenFHE chain success" is stale unless it
  points to a validated integrated artifact with no intermediate decrypts. The
  current evidence supports recurrence smokes, bootstrap probes, pre-recurrence
  and full-layer gate/proxy artifacts, segmented prefix-scan planning, a Stage 1
  prefix-scan smoke, a tiny encrypted MIMO/SSD block smoke, and head-pack/readout
  layout sweeps.
- Stage 1 prefix scan, segmented carry, and prefix-scan smoke are completed and
  should not remain open PBIs.
- Full encrypted vocab argmax remains a Stage 2 research branch; client-side
  decoding is the baseline and not a Stage 0 blocker.
