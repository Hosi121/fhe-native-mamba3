diff --git a/src/PeerUtils.cu b/src/PeerUtils.cu
index 661d8b1..9d6450c 100644
--- a/src/PeerUtils.cu
+++ b/src/PeerUtils.cu
@@ -13,7 +13,7 @@ std::vector<cudaGraphNode_t> get_current_capture_dependencies(cudaStream_t s) {
 	cudaGraphNode_t* depNodes = nullptr;
 	size_t numDeps			  = 0;
 
-	cudaError_t err = cudaStreamGetCaptureInfo(s, &status, nullptr, nullptr, ((const cudaGraphNode_t**)&depNodes), &numDeps);
+	cudaError_t err = cudaStreamGetCaptureInfo(s, &status, nullptr, nullptr, ((const cudaGraphNode_t**)&depNodes), nullptr, &numDeps);
 
 	std::vector<cudaGraphNode_t> deps;
 	if (err == cudaSuccess && numDeps > 0 && depNodes) {
@@ -96,6 +96,7 @@ static void launch(cudaFunction_t kernel, dim3 grid, dim3 block, void** args, ui
 		// NCCL explicitly updates capture dependencies after cuLaunchKernelEx
 		cudaError_t err = cudaStreamUpdateCaptureDependencies(s, // The capturing stream
 		  nullptr,												 // No specific nodes to depend on
+		  nullptr,												 // No edge data (CUDA 13)
 		  0,													 // No additional nodes
 		  cudaStreamAddCaptureDependencies						 // Add implicit dependencies
 		);
@@ -120,6 +121,7 @@ static void launch(cudaFunction_t kernel, dim3 grid, dim3 block, void** args, ui
 		  nullptr, // captureID (optional)
 		  &captured_graph,
 		  (const cudaGraphNode_t**)&deps_after_array,
+		  nullptr, // edge data (CUDA 13)
 		  &numDeps_after);
 
 		if (err != cudaSuccess) {
@@ -175,6 +177,7 @@ static void launch(cudaFunction_t kernel, dim3 grid, dim3 block, void** args, ui
 								err = cudaGraphAddDependencies(captured_graph,
 								  deps_before.data(), // From these nodes
 								  &latest_node,		  // To this kernel node
+								  nullptr,			  // no edge data (CUDA 13)
 								  deps_before.size()  // This many dependencies
 								);
 								CudaCheckErrorModNoSync;
@@ -223,7 +226,7 @@ void pollingKernel(TimelineSemaphore* gpu1_complete_flag, uint64_t value, cudaSt
 		const cudaGraphNode_t* deps;
 		size_t dep_count;
 
-		cudaStreamGetCaptureInfo(s, &capture_status, nullptr, &capturing_graph, &deps, &dep_count);
+		cudaStreamGetCaptureInfo(s, &capture_status, nullptr, &capturing_graph, &deps, nullptr, &dep_count);
 
 		// ========================================================================
 		// Create peer access kernel node with V2 parameters (supports attributes)
@@ -256,7 +259,7 @@ void pollingKernel(TimelineSemaphore* gpu1_complete_flag, uint64_t value, cudaSt
 		cudaGraphKernelNodeSetAttribute(peer_kernel_node, cudaLaunchAttributeMemSyncDomain, &attr_value);
 
 		// Update stream dependencies so subsequent work depends on peer kernel
-		cudaStreamUpdateCaptureDependencies(s, &peer_kernel_node, 1, 1);
+		cudaStreamUpdateCaptureDependencies(s, &peer_kernel_node, nullptr, 1, 1);
 	}
 }
 
@@ -293,7 +296,7 @@ void notifyKernel(TimelineSemaphore* gpu1_complete_flag, uint64_t value, cudaStr
 		const cudaGraphNode_t* deps;
 		size_t dep_count;
 
-		cudaStreamGetCaptureInfo(s, &capture_status, nullptr, &capturing_graph, &deps, &dep_count);
+		cudaStreamGetCaptureInfo(s, &capture_status, nullptr, &capturing_graph, &deps, nullptr, &dep_count);
 
 		// ========================================================================
 		// Create peer access kernel node with V2 parameters (supports attributes)
@@ -326,7 +329,7 @@ void notifyKernel(TimelineSemaphore* gpu1_complete_flag, uint64_t value, cudaStr
 		cudaGraphKernelNodeSetAttribute(peer_kernel_node, cudaLaunchAttributeMemSyncDomain, &attr_value);
 
 		// Update stream dependencies so subsequent work depends on peer kernel
-		cudaStreamUpdateCaptureDependencies(s, &peer_kernel_node, 1, 1);
+		cudaStreamUpdateCaptureDependencies(s, &peer_kernel_node, nullptr, 1, 1);
 	}
 }
 
@@ -423,19 +426,19 @@ void printGraphDependencies(cudaGraph_t graph, const char* name) {
 
 		// Incoming edges (dependencies)
 		size_t num_incoming;
-		cudaGraphNodeGetDependencies(nodes[i], NULL, &num_incoming);
+		cudaGraphNodeGetDependencies(nodes[i], NULL, NULL, &num_incoming);
 
 		// Outgoing edges (dependents)
 		size_t num_outgoing;
 
-		cudaGraphNodeGetDependentNodes(nodes[i], NULL, &num_outgoing);
+		cudaGraphNodeGetDependentNodes(nodes[i], NULL, NULL, &num_outgoing);
 
 		printf("Node[%zu]: %s | Incoming: %zu | Outgoing: %zu\n", i, getNodeTypeName(type), num_incoming, num_outgoing);
 
 		// Print incoming edges
 		if (num_incoming > 0) {
 			std::vector<cudaGraphNode_t> deps(num_incoming);
-			cudaGraphNodeGetDependencies(nodes[i], deps.data(), &num_incoming);
+			cudaGraphNodeGetDependencies(nodes[i], deps.data(), NULL, &num_incoming);
 
 			printf("  ← Depends on: ");
 			for (size_t j = 0; j < num_incoming; j++) {
@@ -449,7 +452,7 @@ void printGraphDependencies(cudaGraph_t graph, const char* name) {
 		// Print outgoing edges
 		if (num_outgoing > 0) {
 			std::vector<cudaGraphNode_t> dependents(num_outgoing);
-			cudaGraphNodeGetDependentNodes(nodes[i], dependents.data(), &num_outgoing);
+			cudaGraphNodeGetDependentNodes(nodes[i], dependents.data(), NULL, &num_outgoing);
 
 			printf("  → Used by: ");
 			for (size_t j = 0; j < num_outgoing; j++) {
@@ -482,7 +485,7 @@ void printGraphDependencies(cudaGraph_t graph, const char* name) {
 	int source_count = 0;
 	for (size_t i = 0; i < num_nodes; i++) {
 		size_t num_deps;
-		cudaGraphNodeGetDependencies(nodes[i], NULL, &num_deps);
+		cudaGraphNodeGetDependencies(nodes[i], NULL, NULL, &num_deps);
 		if (num_deps == 0) {
 			printf("[%zu] ", i);
 			source_count++;
@@ -495,7 +498,7 @@ void printGraphDependencies(cudaGraph_t graph, const char* name) {
 	int sink_count = 0;
 	for (size_t i = 0; i < num_nodes; i++) {
 		size_t num_dependents;
-		cudaGraphNodeGetDependentNodes(nodes[i], NULL, &num_dependents);
+		cudaGraphNodeGetDependentNodes(nodes[i], NULL, NULL, &num_dependents);
 		if (num_dependents == 0) {
 			printf("[%zu] ", i);
 			sink_count++;
@@ -509,7 +512,7 @@ void printGraphDependencies(cudaGraph_t graph, const char* name) {
 	for (size_t i = 0; i < num_nodes; i++) {
 		size_t num_deps;
 		size_t num_dependents = 0;
-		cudaGraphNodeGetDependentNodes(nodes[i], NULL, &num_deps);
+		cudaGraphNodeGetDependentNodes(nodes[i], NULL, NULL, &num_deps);
 
 		// Orphaned if no incoming AND no outgoing (except sources/sinks)
 		if (num_deps == 0 && num_dependents == 0 && num_nodes > 1) {
@@ -533,11 +536,11 @@ bool pathExists(cudaGraphNode_t source, cudaGraphNode_t sink, const std::vector<
 
 	// Get dependents of source
 	size_t num_dependents;
-	cudaGraphNodeGetDependentNodes(source, NULL, &num_dependents);
+	cudaGraphNodeGetDependentNodes(source, NULL, NULL, &num_dependents);
 
 	if (num_dependents > 0) {
 		std::vector<cudaGraphNode_t> dependents(num_dependents);
-		cudaGraphNodeGetDependentNodes(source, dependents.data(), &num_dependents);
+		cudaGraphNodeGetDependentNodes(source, dependents.data(), NULL, &num_dependents);
 
 		for (auto& dep : dependents) {
 			if (visited.find((uintptr_t)dep) == visited.end()) {
@@ -582,17 +585,17 @@ void printGraphDependencies2(cudaGraph_t graph, const char* name) {
 		cudaGraphNodeGetType(nodes[i], &type);
 		CudaCheckErrorModNoSync;
 		size_t num_incoming;
-		cudaGraphNodeGetDependencies(nodes[i], NULL, &num_incoming);
+		cudaGraphNodeGetDependencies(nodes[i], NULL, NULL, &num_incoming);
 		CudaCheckErrorModNoSync;
 		size_t num_outgoing;
-		cudaGraphNodeGetDependentNodes(nodes[i], NULL, &num_outgoing);
+		cudaGraphNodeGetDependentNodes(nodes[i], NULL, NULL, &num_outgoing);
 		CudaCheckErrorModNoSync;
 		printf("Node[%zu]: %s | Incoming: %zu | Outgoing: %zu\n", i, getNodeTypeName(type), num_incoming, num_outgoing);
 
 		// Print incoming edges
 		if (num_incoming > 0) {
 			std::vector<cudaGraphNode_t> deps(num_incoming);
-			cudaGraphNodeGetDependencies(nodes[i], deps.data(), &num_incoming);
+			cudaGraphNodeGetDependencies(nodes[i], deps.data(), NULL, &num_incoming);
 			CudaCheckErrorModNoSync;
 			printf("  ← Depends on: ");
 			for (size_t j = 0; j < num_incoming; j++) {
@@ -606,7 +609,7 @@ void printGraphDependencies2(cudaGraph_t graph, const char* name) {
 		// Print outgoing edges
 		if (num_outgoing > 0) {
 			std::vector<cudaGraphNode_t> dependents(num_outgoing);
-			cudaGraphNodeGetDependentNodes(nodes[i], dependents.data(), &num_outgoing);
+			cudaGraphNodeGetDependentNodes(nodes[i], dependents.data(), NULL, &num_outgoing);
 			CudaCheckErrorModNoSync;
 			printf("  → Used by: ");
 			for (size_t j = 0; j < num_outgoing; j++) {
@@ -635,7 +638,7 @@ void printGraphDependencies2(cudaGraph_t graph, const char* name) {
 	printf("Source nodes: ");
 	for (size_t i = 0; i < num_nodes; i++) {
 		size_t num_deps;
-		cudaGraphNodeGetDependencies(nodes[i], NULL, &num_deps);
+		cudaGraphNodeGetDependencies(nodes[i], NULL, NULL, &num_deps);
 		if (num_deps == 0) {
 			printf("[%zu] ", i);
 			source_nodes.push_back(i);
@@ -648,7 +651,7 @@ void printGraphDependencies2(cudaGraph_t graph, const char* name) {
 	printf("Sink nodes: ");
 	for (size_t i = 0; i < num_nodes; i++) {
 		size_t num_dependents;
-		cudaGraphNodeGetDependentNodes(nodes[i], NULL, &num_dependents);
+		cudaGraphNodeGetDependentNodes(nodes[i], NULL, NULL, &num_dependents);
 		if (num_dependents == 0) {
 			printf("[%zu] ", i);
 			sink_nodes.push_back(i);
@@ -665,13 +668,13 @@ void printGraphDependencies2(cudaGraph_t graph, const char* name) {
 
 		for (size_t sink_idx : sink_nodes) {
 			size_t num_incoming;
-			cudaGraphNodeGetDependencies(nodes[sink_idx], NULL, &num_incoming);
+			cudaGraphNodeGetDependencies(nodes[sink_idx], NULL, NULL, &num_incoming);
 			CudaCheckErrorModNoSync;
 			printf("Sink[%zu]: Has %zu incoming dependencies\n", sink_idx, num_incoming);
 
 			// Get dependencies for this sink
 			std::vector<cudaGraphNode_t> deps(num_incoming);
-			cudaGraphNodeGetDependencies(nodes[sink_idx], deps.data(), &num_incoming);
+			cudaGraphNodeGetDependencies(nodes[sink_idx], deps.data(), NULL, &num_incoming);
 			CudaCheckErrorModNoSync;
 			printf("  Depends on: ");
 			for (size_t i = 0; i < num_incoming; i++) {
@@ -756,7 +759,7 @@ void transferKernel(float* src, float* dst, size_t elems, cudaStream_t s, int sr
 		cudaStreamCaptureStatus _capture_status;
 		const cudaGraphNode_t* _deps;
 		size_t _dep_count;
-		cudaStreamGetCaptureInfo(s, &_capture_status, nullptr, &_capturing_graph, &_deps, &_dep_count);
+		cudaStreamGetCaptureInfo(s, &_capture_status, nullptr, &_capturing_graph, &_deps, nullptr, &_dep_count);
 
 		cudaGraphNode_t copy_0to1;
 		cudaMemcpy3DParms memcpyParams = { 0 };
@@ -774,7 +777,7 @@ void transferKernel(float* src, float* dst, size_t elems, cudaStream_t s, int sr
 
 		cudaGraphAddMemcpyNode(&copy_0to1, _capturing_graph, _deps, _dep_count, &memcpyParams);
 
-		cudaStreamUpdateCaptureDependencies(s, &copy_0to1, 1, 1);
+		cudaStreamUpdateCaptureDependencies(s, &copy_0to1, nullptr, 1, 1);
 
 	} else if (!is_stream_being_captured(s)) {
 		// p2p_transfer_1d<<<involved_sm, 128, 0, s>>>(src, dst, elems);
@@ -803,7 +806,7 @@ void transferKernel(float* src, float* dst, size_t elems, cudaStream_t s, int sr
 		const cudaGraphNode_t* deps;
 		size_t dep_count;
 
-		cudaStreamGetCaptureInfo(s, &capture_status, nullptr, &capturing_graph, &deps, &dep_count);
+		cudaStreamGetCaptureInfo(s, &capture_status, nullptr, &capturing_graph, &deps, nullptr, &dep_count);
 
 		// ========================================================================
 		// Create peer access kernel node with V2 parameters (supports attributes)
@@ -836,7 +839,7 @@ void transferKernel(float* src, float* dst, size_t elems, cudaStream_t s, int sr
 		cudaGraphKernelNodeSetAttribute(peer_kernel_node, cudaLaunchAttributeMemSyncDomain, &attr_value);
 
 		// Update stream dependencies so subsequent work depends on peer kernel
-		cudaStreamUpdateCaptureDependencies(s, &peer_kernel_node, 1, 1);
+		cudaStreamUpdateCaptureDependencies(s, &peer_kernel_node, nullptr, 1, 1);
 	}
 }
 
